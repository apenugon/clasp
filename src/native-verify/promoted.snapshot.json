{"loweredValue":"const hello = literal:Hello from Clasp","loweredFunction":"function id(value) = name:value","loweredModule":"const hello = literal:Hello from Clasp\nfunction id(value) = name:value\nconst main = call id(name:hello)","checkedValueType":"Str","checkedFunctionType":"Str -> Str","checkedModule":"hello : Str\nid : Str -> Str\nmain : Str","explainModule":"hello : Str\nid : Str -> Str\nmain : Str\n\nconst hello = literal:Hello from Clasp\nfunction id(value) = name:value\nconst main = call id(name:hello)","mismatchDiagnostic":"In `greeting`: Type mismatch for `\"hello\"`: expected Int, found Str.","emittedModule":"// Generated by compiler-selfhost\nfunction $claspSnapshotValue(value) {\n  if (typeof globalThis.structuredClone === \"function\") {\n    return globalThis.structuredClone(value);\n  }\n  return value === undefined ? undefined : JSON.parse(JSON.stringify(value));\n}\n\nfunction $claspExpectObject(value, path = \"value\") {\n  if (typeof value !== \"object\" || value === null || Array.isArray(value)) {\n    throw new Error(`${path} expected an object`);\n  }\n  return value;\n}\n\nfunction $claspExpectArray(value, path = \"value\") {\n  if (!Array.isArray(value)) {\n    throw new Error(`${path} expected an array`);\n  }\n  return value;\n}\n\nfunction $claspExpectString(value, path = \"value\") {\n  if (typeof value !== \"string\") {\n    throw new Error(`${path} expected a string`);\n  }\n  return value;\n}\n\nfunction $claspExpectInt(value, path = \"value\") {\n  if (!Number.isInteger(value)) {\n    throw new Error(`${path} expected an integer`);\n  }\n  return value;\n}\n\nfunction $claspExpectBool(value, path = \"value\") {\n  if (typeof value !== \"boolean\") {\n    throw new Error(`${path} expected a boolean`);\n  }\n  return value;\n}\n\nconst $claspBinaryMagic = Object.freeze([67, 76, 66, 49]);\nconst $claspTextEncoder = typeof TextEncoder === \"undefined\" ? null : new TextEncoder();\nconst $claspTextDecoder = typeof TextDecoder === \"undefined\" ? null : new TextDecoder();\n\nfunction $claspEncodeUtf8Text(value) {\n  if ($claspTextEncoder === null) {\n    throw new Error(\"This runtime does not provide TextEncoder for binary Clasp codecs\");\n  }\n  return $claspTextEncoder.encode($claspExpectString(value, \"value\"));\n}\n\nfunction $claspDecodeUtf8Text(bytes, path = \"value\") {\n  if ($claspTextDecoder === null) {\n    throw new Error(\"This runtime does not provide TextDecoder for binary Clasp codecs\");\n  }\n  return $claspTextDecoder.decode($claspNormalizeBinaryBytes(bytes, path));\n}\n\nfunction $claspNormalizeBinaryBytes(value, path = \"value\") {\n  if (value instanceof Uint8Array) {\n    return value;\n  }\n  if (value instanceof ArrayBuffer) {\n    return new Uint8Array(value);\n  }\n  if (ArrayBuffer.isView(value)) {\n    return new Uint8Array(value.buffer, value.byteOffset, value.byteLength);\n  }\n  throw new Error(`${path} expected binary bytes`);\n}\n\nfunction $claspWriteUint32(target, offset, value) {\n  new DataView(target.buffer, target.byteOffset, target.byteLength).setUint32(offset, value, true);\n}\n\nfunction $claspReadUint32(source, offset, path = \"value\") {\n  if (offset + 4 > source.byteLength) {\n    throw new Error(`${path} truncated binary payload`);\n  }\n  return new DataView(source.buffer, source.byteOffset, source.byteLength).getUint32(offset, true);\n}\n\nfunction $claspFrameBinaryPayload(payload) {\n  const bytes = $claspNormalizeBinaryBytes(payload, \"payload\");\n  const frame = new Uint8Array(4 + bytes.byteLength);\n  $claspWriteUint32(frame, 0, bytes.byteLength);\n  frame.set(bytes, 4);\n  return frame;\n}\n\nfunction $claspUnframeBinaryPayload(frame, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(frame, path);\n  const expectedLength = $claspReadUint32(bytes, 0, path);\n  const payload = bytes.slice(4);\n  if (payload.byteLength !== expectedLength) {\n    throw new Error(`${path} expected ${expectedLength} framed bytes but received ${payload.byteLength}`);\n  }\n  return payload;\n}\n\nfunction $claspEncodeSchemaBinaryPayload(schemaContract, value) {\n  const normalizedValue = schemaContract.toHost(schemaContract.fromHost(value, \"value\"), \"value\");\n  const fingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  const fingerprintBytes = $claspEncodeUtf8Text(fingerprint);\n  const payloadBytes = $claspEncodeUtf8Text(JSON.stringify(normalizedValue));\n  const buffer = new Uint8Array(12 + fingerprintBytes.byteLength + payloadBytes.byteLength);\n  buffer.set($claspBinaryMagic, 0);\n  $claspWriteUint32(buffer, 4, fingerprintBytes.byteLength);\n  $claspWriteUint32(buffer, 8, payloadBytes.byteLength);\n  buffer.set(fingerprintBytes, 12);\n  buffer.set(payloadBytes, 12 + fingerprintBytes.byteLength);\n  return buffer;\n}\n\nfunction $claspDecodeSchemaBinaryPayload(schemaContract, payload, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(payload, path);\n  if (bytes.byteLength < 12) {\n    throw new Error(`${path} expected a Clasp binary payload`);\n  }\n  for (let index = 0; index < $claspBinaryMagic.length; index += 1) {\n    if (bytes[index] !== $claspBinaryMagic[index]) {\n      throw new Error(`${path} expected a Clasp binary payload`);\n    }\n  }\n  const fingerprintLength = $claspReadUint32(bytes, 4, path);\n  const payloadLength = $claspReadUint32(bytes, 8, path);\n  const payloadOffset = 12 + fingerprintLength;\n  if (payloadOffset + payloadLength !== bytes.byteLength) {\n    throw new Error(`${path} has an invalid Clasp binary payload length`);\n  }\n  const fingerprint = $claspDecodeUtf8Text(bytes.slice(12, payloadOffset), `${path}.fingerprint`);\n  const expectedFingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  if (fingerprint !== expectedFingerprint) {\n    throw new Error(`${path} expected schema fingerprint ${expectedFingerprint} but received ${fingerprint}`);\n  }\n  const payloadText = $claspDecodeUtf8Text(bytes.slice(payloadOffset), `${path}.payload`);\n  return schemaContract.toHost(schemaContract.fromHost(JSON.parse(payloadText), path), path);\n}\n\nfunction $claspCreateSchemaBinaryCodec(schemaContract) {\n  return Object.freeze({\n    type: schemaContract.type ?? schemaContract.name ?? null,\n    schemaFingerprint: String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\"),\n    framing: \"length_prefixed\",\n    encode(value) { return $claspEncodeSchemaBinaryPayload(schemaContract, value); },\n    decode(bytes, path = \"value\") { return $claspDecodeSchemaBinaryPayload(schemaContract, bytes, path); },\n    frame(value) { return $claspFrameBinaryPayload(this.encode(value)); },\n    unframe(frame, path = \"value\") { return this.decode($claspUnframeBinaryPayload(frame, path), path); }\n  });\n}\n\nfunction $claspCreateSchemaStream(contract, initial = null) {\n  return Object.freeze({ kind: \"clasp-schema-stream\", schema: contract, initial });\n}\n\nfunction $claspPromptMessage(role, content) {\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([Object.freeze({\n      role: $claspExpectString(role, \"prompt.role\"),\n      content: $claspExpectString(content, \"prompt.content\")\n    })])\n  });\n}\n\nfunction $claspExpectPrompt(value, path = \"value\") {\n  const objectValue = $claspExpectObject(value, path);\n  if (objectValue.$kind !== \"prompt\") {\n    throw new Error(`${path} expected a Prompt value`);\n  }\n  const messages = $claspExpectArray(objectValue.messages, `${path}.messages`).map((message, index) => {\n    const messagePath = `${path}.messages[${index}]`;\n    const messageObject = $claspExpectObject(message, messagePath);\n    return Object.freeze({\n      role: $claspExpectString(messageObject.role, `${messagePath}.role`),\n      content: $claspExpectString(messageObject.content, `${messagePath}.content`)\n    });\n  });\n  return Object.freeze({ $kind: \"prompt\", messages: Object.freeze(messages) });\n}\n\nfunction systemPrompt(content) { return $claspPromptMessage(\"system\", content); }\nfunction assistantPrompt(content) { return $claspPromptMessage(\"assistant\", content); }\nfunction userPrompt(content) { return $claspPromptMessage(\"user\", content); }\n\nfunction appendPrompt(left, right) {\n  const leftPrompt = $claspExpectPrompt(left, \"left\");\n  const rightPrompt = $claspExpectPrompt(right, \"right\");\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([...leftPrompt.messages, ...rightPrompt.messages].map((message) => Object.freeze({ role: message.role, content: message.content })))\n  });\n}\n\nfunction promptText(value) {\n  const prompt = $claspExpectPrompt(value, \"value\");\n  return prompt.messages.map((message) => `${message.role}: ${message.content}`).join(\"\\n\\n\");\n}\n\nfunction $claspCreateTraceCollector(seed = []) {\n  const entries = Array.isArray(seed) ? seed.map((entry) => $claspSnapshotValue(entry)) : [];\n  return Object.freeze({\n    kind: \"clasp-trace-collector\",\n    version: 1,\n    record(entry) {\n      const snapshot = $claspSnapshotValue(entry);\n      entries.push(snapshot);\n      return snapshot;\n    },\n    entries() {\n      return $claspSnapshotValue(entries);\n    },\n    clear() {\n      entries.length = 0;\n      return this.entries();\n    }\n  });\n}\n\nfunction $claspMaybeRecordTrace(options = null, kind, name, action) {\n  const collector = options && typeof options === \"object\" && options.collector && typeof options.collector.record === \"function\"\n    ? options.collector\n    : null;\n  if (collector) {\n    collector.record(Object.freeze({ kind, name, action, status: \"ok\" }));\n  }\n}\nexport const hello = \"Hello from Clasp\";\nexport function id(value) { return value; }\nexport const main = id(hello);\nconst __claspExports = Object.freeze({ hello: hello, id: id, main: main });\n\nexport const __claspSchemas = Object.freeze({  });\nfor (const schemaContract of Object.values(__claspSchemas)) {\n  schemaContract.binary = $claspCreateSchemaBinaryCodec(schemaContract);\n  schemaContract.encodeBinary = function (value) { return this.binary.encode(value); };\n  schemaContract.decodeBinary = function (payload, path = \"value\") { return this.binary.decode(payload, path); };\n  schemaContract.encodeFramedBinary = function (value) { return this.binary.frame(value); };\n  schemaContract.decodeFramedBinary = function (frame, path = \"value\") { return this.binary.unframe(frame, path); };\n  Object.freeze(schemaContract);\n}\n\nexport const __claspTraceCollector = Object.freeze({\n  version: 1,\n  create(seed = []) { return $claspCreateTraceCollector(seed); }\n});\n\nexport const __claspModule = Object.freeze({\n  name: \"Main\",\n  version: 1\n});\n\nexport const __claspBindings = Object.freeze({\n  kind: \"clasp-generated-bindings\",\n  version: 1,\n  module: __claspModule,\n  schemas: __claspSchemas,\n  traces: __claspTraceCollector\n});\n","candidateEmittedModule":"// Generated by compiler-selfhost\nfunction $claspSnapshotValue(value) {\n  if (typeof globalThis.structuredClone === \"function\") {\n    return globalThis.structuredClone(value);\n  }\n  return value === undefined ? undefined : JSON.parse(JSON.stringify(value));\n}\n\nfunction $claspExpectObject(value, path = \"value\") {\n  if (typeof value !== \"object\" || value === null || Array.isArray(value)) {\n    throw new Error(`${path} expected an object`);\n  }\n  return value;\n}\n\nfunction $claspExpectArray(value, path = \"value\") {\n  if (!Array.isArray(value)) {\n    throw new Error(`${path} expected an array`);\n  }\n  return value;\n}\n\nfunction $claspExpectString(value, path = \"value\") {\n  if (typeof value !== \"string\") {\n    throw new Error(`${path} expected a string`);\n  }\n  return value;\n}\n\nfunction $claspExpectInt(value, path = \"value\") {\n  if (!Number.isInteger(value)) {\n    throw new Error(`${path} expected an integer`);\n  }\n  return value;\n}\n\nfunction $claspExpectBool(value, path = \"value\") {\n  if (typeof value !== \"boolean\") {\n    throw new Error(`${path} expected a boolean`);\n  }\n  return value;\n}\n\nconst $claspBinaryMagic = Object.freeze([67, 76, 66, 49]);\nconst $claspTextEncoder = typeof TextEncoder === \"undefined\" ? null : new TextEncoder();\nconst $claspTextDecoder = typeof TextDecoder === \"undefined\" ? null : new TextDecoder();\n\nfunction $claspEncodeUtf8Text(value) {\n  if ($claspTextEncoder === null) {\n    throw new Error(\"This runtime does not provide TextEncoder for binary Clasp codecs\");\n  }\n  return $claspTextEncoder.encode($claspExpectString(value, \"value\"));\n}\n\nfunction $claspDecodeUtf8Text(bytes, path = \"value\") {\n  if ($claspTextDecoder === null) {\n    throw new Error(\"This runtime does not provide TextDecoder for binary Clasp codecs\");\n  }\n  return $claspTextDecoder.decode($claspNormalizeBinaryBytes(bytes, path));\n}\n\nfunction $claspNormalizeBinaryBytes(value, path = \"value\") {\n  if (value instanceof Uint8Array) {\n    return value;\n  }\n  if (value instanceof ArrayBuffer) {\n    return new Uint8Array(value);\n  }\n  if (ArrayBuffer.isView(value)) {\n    return new Uint8Array(value.buffer, value.byteOffset, value.byteLength);\n  }\n  throw new Error(`${path} expected binary bytes`);\n}\n\nfunction $claspWriteUint32(target, offset, value) {\n  new DataView(target.buffer, target.byteOffset, target.byteLength).setUint32(offset, value, true);\n}\n\nfunction $claspReadUint32(source, offset, path = \"value\") {\n  if (offset + 4 > source.byteLength) {\n    throw new Error(`${path} truncated binary payload`);\n  }\n  return new DataView(source.buffer, source.byteOffset, source.byteLength).getUint32(offset, true);\n}\n\nfunction $claspFrameBinaryPayload(payload) {\n  const bytes = $claspNormalizeBinaryBytes(payload, \"payload\");\n  const frame = new Uint8Array(4 + bytes.byteLength);\n  $claspWriteUint32(frame, 0, bytes.byteLength);\n  frame.set(bytes, 4);\n  return frame;\n}\n\nfunction $claspUnframeBinaryPayload(frame, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(frame, path);\n  const expectedLength = $claspReadUint32(bytes, 0, path);\n  const payload = bytes.slice(4);\n  if (payload.byteLength !== expectedLength) {\n    throw new Error(`${path} expected ${expectedLength} framed bytes but received ${payload.byteLength}`);\n  }\n  return payload;\n}\n\nfunction $claspEncodeSchemaBinaryPayload(schemaContract, value) {\n  const normalizedValue = schemaContract.toHost(schemaContract.fromHost(value, \"value\"), \"value\");\n  const fingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  const fingerprintBytes = $claspEncodeUtf8Text(fingerprint);\n  const payloadBytes = $claspEncodeUtf8Text(JSON.stringify(normalizedValue));\n  const buffer = new Uint8Array(12 + fingerprintBytes.byteLength + payloadBytes.byteLength);\n  buffer.set($claspBinaryMagic, 0);\n  $claspWriteUint32(buffer, 4, fingerprintBytes.byteLength);\n  $claspWriteUint32(buffer, 8, payloadBytes.byteLength);\n  buffer.set(fingerprintBytes, 12);\n  buffer.set(payloadBytes, 12 + fingerprintBytes.byteLength);\n  return buffer;\n}\n\nfunction $claspDecodeSchemaBinaryPayload(schemaContract, payload, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(payload, path);\n  if (bytes.byteLength < 12) {\n    throw new Error(`${path} expected a Clasp binary payload`);\n  }\n  for (let index = 0; index < $claspBinaryMagic.length; index += 1) {\n    if (bytes[index] !== $claspBinaryMagic[index]) {\n      throw new Error(`${path} expected a Clasp binary payload`);\n    }\n  }\n  const fingerprintLength = $claspReadUint32(bytes, 4, path);\n  const payloadLength = $claspReadUint32(bytes, 8, path);\n  const payloadOffset = 12 + fingerprintLength;\n  if (payloadOffset + payloadLength !== bytes.byteLength) {\n    throw new Error(`${path} has an invalid Clasp binary payload length`);\n  }\n  const fingerprint = $claspDecodeUtf8Text(bytes.slice(12, payloadOffset), `${path}.fingerprint`);\n  const expectedFingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  if (fingerprint !== expectedFingerprint) {\n    throw new Error(`${path} expected schema fingerprint ${expectedFingerprint} but received ${fingerprint}`);\n  }\n  const payloadText = $claspDecodeUtf8Text(bytes.slice(payloadOffset), `${path}.payload`);\n  return schemaContract.toHost(schemaContract.fromHost(JSON.parse(payloadText), path), path);\n}\n\nfunction $claspCreateSchemaBinaryCodec(schemaContract) {\n  return Object.freeze({\n    type: schemaContract.type ?? schemaContract.name ?? null,\n    schemaFingerprint: String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\"),\n    framing: \"length_prefixed\",\n    encode(value) { return $claspEncodeSchemaBinaryPayload(schemaContract, value); },\n    decode(bytes, path = \"value\") { return $claspDecodeSchemaBinaryPayload(schemaContract, bytes, path); },\n    frame(value) { return $claspFrameBinaryPayload(this.encode(value)); },\n    unframe(frame, path = \"value\") { return this.decode($claspUnframeBinaryPayload(frame, path), path); }\n  });\n}\n\nfunction $claspCreateSchemaStream(contract, initial = null) {\n  return Object.freeze({ kind: \"clasp-schema-stream\", schema: contract, initial });\n}\n\nfunction $claspPromptMessage(role, content) {\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([Object.freeze({\n      role: $claspExpectString(role, \"prompt.role\"),\n      content: $claspExpectString(content, \"prompt.content\")\n    })])\n  });\n}\n\nfunction $claspExpectPrompt(value, path = \"value\") {\n  const objectValue = $claspExpectObject(value, path);\n  if (objectValue.$kind !== \"prompt\") {\n    throw new Error(`${path} expected a Prompt value`);\n  }\n  const messages = $claspExpectArray(objectValue.messages, `${path}.messages`).map((message, index) => {\n    const messagePath = `${path}.messages[${index}]`;\n    const messageObject = $claspExpectObject(message, messagePath);\n    return Object.freeze({\n      role: $claspExpectString(messageObject.role, `${messagePath}.role`),\n      content: $claspExpectString(messageObject.content, `${messagePath}.content`)\n    });\n  });\n  return Object.freeze({ $kind: \"prompt\", messages: Object.freeze(messages) });\n}\n\nfunction systemPrompt(content) { return $claspPromptMessage(\"system\", content); }\nfunction assistantPrompt(content) { return $claspPromptMessage(\"assistant\", content); }\nfunction userPrompt(content) { return $claspPromptMessage(\"user\", content); }\n\nfunction appendPrompt(left, right) {\n  const leftPrompt = $claspExpectPrompt(left, \"left\");\n  const rightPrompt = $claspExpectPrompt(right, \"right\");\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([...leftPrompt.messages, ...rightPrompt.messages].map((message) => Object.freeze({ role: message.role, content: message.content })))\n  });\n}\n\nfunction promptText(value) {\n  const prompt = $claspExpectPrompt(value, \"value\");\n  return prompt.messages.map((message) => `${message.role}: ${message.content}`).join(\"\\n\\n\");\n}\n\nfunction $claspCreateTraceCollector(seed = []) {\n  const entries = Array.isArray(seed) ? seed.map((entry) => $claspSnapshotValue(entry)) : [];\n  return Object.freeze({\n    kind: \"clasp-trace-collector\",\n    version: 1,\n    record(entry) {\n      const snapshot = $claspSnapshotValue(entry);\n      entries.push(snapshot);\n      return snapshot;\n    },\n    entries() {\n      return $claspSnapshotValue(entries);\n    },\n    clear() {\n      entries.length = 0;\n      return this.entries();\n    }\n  });\n}\n\nfunction $claspMaybeRecordTrace(options = null, kind, name, action) {\n  const collector = options && typeof options === \"object\" && options.collector && typeof options.collector.record === \"function\"\n    ? options.collector\n    : null;\n  if (collector) {\n    collector.record(Object.freeze({ kind, name, action, status: \"ok\" }));\n  }\n}\nexport const hello = \"Hello from Clasp\";\nexport function id(value) { return value; }\nexport const main = id(hello);\nconst __claspExports = Object.freeze({ hello: hello, id: id, main: main });\n\nexport const __claspSchemas = Object.freeze({  });\nfor (const schemaContract of Object.values(__claspSchemas)) {\n  schemaContract.binary = $claspCreateSchemaBinaryCodec(schemaContract);\n  schemaContract.encodeBinary = function (value) { return this.binary.encode(value); };\n  schemaContract.decodeBinary = function (payload, path = \"value\") { return this.binary.decode(payload, path); };\n  schemaContract.encodeFramedBinary = function (value) { return this.binary.frame(value); };\n  schemaContract.decodeFramedBinary = function (frame, path = \"value\") { return this.binary.unframe(frame, path); };\n  Object.freeze(schemaContract);\n}\n\nexport const __claspTraceCollector = Object.freeze({\n  version: 1,\n  create(seed = []) { return $claspCreateTraceCollector(seed); }\n});\n\nexport const __claspModule = Object.freeze({\n  name: \"Main\",\n  version: 1\n});\n\nexport const __claspBindings = Object.freeze({\n  kind: \"clasp-generated-bindings\",\n  version: 1,\n  module: __claspModule,\n  schemas: __claspSchemas,\n  traces: __claspTraceCollector\n});\n","emittedNativeModule":"format clasp-native-ir-v1\nmodule Main\nexports [hello, id, main]\nentrypoints [hello{symbol=clasp_native__Main__hello, kind=global, arity=0}, id{symbol=clasp_native__Main__id, kind=function, arity=1}, main{symbol=clasp_native__Main__main, kind=global, arity=0}]\n\nabi {\n  version clasp-native-v1\n  word_bytes 8\n  memory_strategy reference_counting\n  allocation { immediate = stack, handle = heap, global = static }\n  ownership_rules [caller_owns_returns, callee_borrows_arguments, aggregates_retain_handle_fields, globals_are_static_roots]\n  root_discovery_rules [static_globals, stack_handle_slots, heap_object_layouts]\n  lifetime_invariants [layout_and_retain_headers, declared_root_offsets_only, release_children_before_free, borrowed_handles_do_not_retain]\n  builtin_layout Int { storage = immediate, words = 1 }\n  builtin_layout Bool { storage = immediate, words = 1 }\n  builtin_layout Str { storage = handle, words = 1 }\n  builtin_layout List { storage = handle, words = 1 }\n  builtin_layout Page { storage = handle, words = 1 }\n  builtin_layout Redirect { storage = handle, words = 1 }\n  builtin_layout View { storage = handle, words = 1 }\n  builtin_layout Prompt { storage = handle, words = 1 }\n  record_layout Principal { words = 1, fields = [id:Str@word0/handle] }\n  record_layout Tenant { words = 1, fields = [id:Str@word0/handle] }\n  record_layout ResourceIdentity { words = 2, fields = [resourceType:Str@word0/handle, resourceId:Str@word1/handle] }\n  record_layout AuditActor { words = 2, fields = [actorType:Str@word0/handle, actorId:Str@word1/handle] }\n  record_layout AuditAction { words = 2, fields = [actionType:Str@word0/handle, summary:Str@word1/handle] }\n  record_layout AuditProvenance { words = 3, fields = [source:Str@word0/handle, requestId:Str@word1/handle, traceId:Str@word2/handle] }\n  record_layout SqliteConnection { words = 4, fields = [id:Str@word0/handle, databasePath:Str@word1/handle, readOnly:Bool@word2/immediate, memory:Bool@word3/immediate] }\n  record_layout AuthSession { words = 4, fields = [sessionId:Str@word0/handle, principal:Principal@word1/handle, tenant:Tenant@word2/handle, resource:ResourceIdentity@word3/handle] }\n  record_layout StandardAuditEnvelope { words = 5, fields = [actor:AuditActor@word0/handle, resource:ResourceIdentity@word1/handle, action:AuditAction@word2/handle, timestamp:Int@word3/immediate, provenance:AuditProvenance@word4/handle] }\n  object_layout Principal { kind = record, header_words = 2, words = 3, roots = [2] }\n  object_layout Tenant { kind = record, header_words = 2, words = 3, roots = [2] }\n  object_layout ResourceIdentity { kind = record, header_words = 2, words = 4, roots = [2, 3] }\n  object_layout AuditActor { kind = record, header_words = 2, words = 4, roots = [2, 3] }\n  object_layout AuditAction { kind = record, header_words = 2, words = 4, roots = [2, 3] }\n  object_layout AuditProvenance { kind = record, header_words = 2, words = 5, roots = [2, 3, 4] }\n  object_layout SqliteConnection { kind = record, header_words = 2, words = 6, roots = [2, 3] }\n  object_layout AuthSession { kind = record, header_words = 2, words = 6, roots = [2, 3, 4, 5] }\n  object_layout StandardAuditEnvelope { kind = record, header_words = 2, words = 7, roots = [2, 3, 4, 6] }\n}\n\nruntime {\n  profile compiler_backend_minimal\n  artifacts [\"runtime/clasp_runtime.h\", \"runtime/clasp_runtime.rs\"]\n  memory_symbols [clasp_rt_init, clasp_rt_register_static_root, clasp_rt_alloc_object, clasp_rt_retain, clasp_rt_release, clasp_rt_string_from_utf8, clasp_rt_bytes_new, clasp_rt_string_list_new, clasp_rt_result_ok_string, clasp_rt_result_err_string, clasp_rt_json_from_string, clasp_rt_json_to_string, clasp_rt_binary_from_json, clasp_rt_json_from_binary, clasp_rt_transport_frame, clasp_rt_transport_unframe]\n  bindings []\n  json_codecs []\n  binary_codecs []\n  boundaries []\n  service_transports []\n}\n\nglobal hello = string(\"Hello from Clasp\")\n\nfunction id(value) = local(value)\n\nglobal main = call(local(id), [local(hello)])\n\n","emittedNativeImageModule":"{\"format\": \"clasp-native-image-v1\", \"irFormat\": \"clasp-native-ir-v1\", \"module\": \"Main\", \"exports\": [\"hello\", \"id\", \"main\"], \"entrypoints\": [{\"name\": \"hello\", \"symbol\": \"clasp_native__Main__hello\", \"kind\": \"global\", \"arity\": 0}, {\"name\": \"id\", \"symbol\": \"clasp_native__Main__id\", \"kind\": \"function\", \"arity\": 1}, {\"name\": \"main\", \"symbol\": \"clasp_native__Main__main\", \"kind\": \"global\", \"arity\": 0}], \"abi\": {\"version\": \"clasp-native-v1\", \"wordBytes\": 8, \"memoryStrategy\": \"reference_counting\", \"allocation\": {\"immediate\": \"stack\", \"handle\": \"heap\", \"global\": \"static\"}, \"ownershipRules\": [\"caller_owns_returns\", \"callee_borrows_arguments\", \"aggregates_retain_handle_fields\", \"globals_are_static_roots\"], \"rootDiscoveryRules\": [\"static_globals\", \"stack_handle_slots\", \"heap_object_layouts\"], \"lifetimeInvariants\": [\"layout_and_retain_headers\", \"declared_root_offsets_only\", \"release_children_before_free\", \"borrowed_handles_do_not_retain\"], \"builtinLayouts\": [{\"name\": \"Int\", \"storage\": \"immediate\", \"words\": 1}, {\"name\": \"Bool\", \"storage\": \"immediate\", \"words\": 1}, {\"name\": \"Str\", \"storage\": \"handle\", \"words\": 1}, {\"name\": \"List\", \"storage\": \"handle\", \"words\": 1}, {\"name\": \"Page\", \"storage\": \"handle\", \"words\": 1}, {\"name\": \"Redirect\", \"storage\": \"handle\", \"words\": 1}, {\"name\": \"View\", \"storage\": \"handle\", \"words\": 1}, {\"name\": \"Prompt\", \"storage\": \"handle\", \"words\": 1}], \"recordLayouts\": [{\"name\": \"Principal\", \"words\": 1, \"fields\": [{\"name\": \"id\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 0, \"wordCount\": 1}]}, {\"name\": \"Tenant\", \"words\": 1, \"fields\": [{\"name\": \"id\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 0, \"wordCount\": 1}]}, {\"name\": \"ResourceIdentity\", \"words\": 2, \"fields\": [{\"name\": \"resourceType\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 0, \"wordCount\": 1}, {\"name\": \"resourceId\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 1, \"wordCount\": 1}]}, {\"name\": \"AuditActor\", \"words\": 2, \"fields\": [{\"name\": \"actorType\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 0, \"wordCount\": 1}, {\"name\": \"actorId\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 1, \"wordCount\": 1}]}, {\"name\": \"AuditAction\", \"words\": 2, \"fields\": [{\"name\": \"actionType\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 0, \"wordCount\": 1}, {\"name\": \"summary\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 1, \"wordCount\": 1}]}, {\"name\": \"AuditProvenance\", \"words\": 3, \"fields\": [{\"name\": \"source\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 0, \"wordCount\": 1}, {\"name\": \"requestId\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 1, \"wordCount\": 1}, {\"name\": \"traceId\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 2, \"wordCount\": 1}]}, {\"name\": \"SqliteConnection\", \"words\": 4, \"fields\": [{\"name\": \"id\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 0, \"wordCount\": 1}, {\"name\": \"databasePath\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 1, \"wordCount\": 1}, {\"name\": \"readOnly\", \"type\": \"Bool\", \"storage\": \"immediate\", \"wordOffset\": 2, \"wordCount\": 1}, {\"name\": \"memory\", \"type\": \"Bool\", \"storage\": \"immediate\", \"wordOffset\": 3, \"wordCount\": 1}]}, {\"name\": \"AuthSession\", \"words\": 4, \"fields\": [{\"name\": \"sessionId\", \"type\": \"Str\", \"storage\": \"handle\", \"wordOffset\": 0, \"wordCount\": 1}, {\"name\": \"principal\", \"type\": \"Principal\", \"storage\": \"handle\", \"wordOffset\": 1, \"wordCount\": 1}, {\"name\": \"tenant\", \"type\": \"Tenant\", \"storage\": \"handle\", \"wordOffset\": 2, \"wordCount\": 1}, {\"name\": \"resource\", \"type\": \"ResourceIdentity\", \"storage\": \"handle\", \"wordOffset\": 3, \"wordCount\": 1}]}, {\"name\": \"StandardAuditEnvelope\", \"words\": 5, \"fields\": [{\"name\": \"actor\", \"type\": \"AuditActor\", \"storage\": \"handle\", \"wordOffset\": 0, \"wordCount\": 1}, {\"name\": \"resource\", \"type\": \"ResourceIdentity\", \"storage\": \"handle\", \"wordOffset\": 1, \"wordCount\": 1}, {\"name\": \"action\", \"type\": \"AuditAction\", \"storage\": \"handle\", \"wordOffset\": 2, \"wordCount\": 1}, {\"name\": \"timestamp\", \"type\": \"Int\", \"storage\": \"immediate\", \"wordOffset\": 3, \"wordCount\": 1}, {\"name\": \"provenance\", \"type\": \"AuditProvenance\", \"storage\": \"handle\", \"wordOffset\": 4, \"wordCount\": 1}]}], \"variantLayouts\": [], \"objectLayouts\": [{\"name\": \"Principal\", \"kind\": \"record\", \"headerWords\": 2, \"words\": 3, \"roots\": [2]}, {\"name\": \"Tenant\", \"kind\": \"record\", \"headerWords\": 2, \"words\": 3, \"roots\": [2]}, {\"name\": \"ResourceIdentity\", \"kind\": \"record\", \"headerWords\": 2, \"words\": 4, \"roots\": [2, 3]}, {\"name\": \"AuditActor\", \"kind\": \"record\", \"headerWords\": 2, \"words\": 4, \"roots\": [2, 3]}, {\"name\": \"AuditAction\", \"kind\": \"record\", \"headerWords\": 2, \"words\": 4, \"roots\": [2, 3]}, {\"name\": \"AuditProvenance\", \"kind\": \"record\", \"headerWords\": 2, \"words\": 5, \"roots\": [2, 3, 4]}, {\"name\": \"SqliteConnection\", \"kind\": \"record\", \"headerWords\": 2, \"words\": 6, \"roots\": [2, 3]}, {\"name\": \"AuthSession\", \"kind\": \"record\", \"headerWords\": 2, \"words\": 6, \"roots\": [2, 3, 4, 5]}, {\"name\": \"StandardAuditEnvelope\", \"kind\": \"record\", \"headerWords\": 2, \"words\": 7, \"roots\": [2, 3, 4, 6]}]}, \"runtime\": {\"profile\": \"compiler_backend_minimal\", \"artifacts\": [\"runtime/clasp_runtime.h\", \"runtime/clasp_runtime.rs\"], \"memorySymbols\": [\"clasp_rt_init\", \"clasp_rt_register_static_root\", \"clasp_rt_alloc_object\", \"clasp_rt_retain\", \"clasp_rt_release\", \"clasp_rt_string_from_utf8\", \"clasp_rt_bytes_new\", \"clasp_rt_string_list_new\", \"clasp_rt_result_ok_string\", \"clasp_rt_result_err_string\", \"clasp_rt_json_from_string\", \"clasp_rt_json_to_string\", \"clasp_rt_binary_from_json\", \"clasp_rt_json_from_binary\", \"clasp_rt_transport_frame\", \"clasp_rt_transport_unframe\"], \"bindings\": [], \"jsonCodecs\": [], \"binaryCodecs\": [], \"boundaries\": [], \"serviceTransports\": [], \"metadata\": {\"guides\": [], \"policies\": [], \"agentRoles\": [], \"agents\": []}}, \"compatibility\": {\"kind\": \"clasp-native-compatibility-v1\", \"interfaceFingerprint\": \"native-compat:1ab4e1b504a35c96\", \"acceptedPreviousFingerprints\": [\"native-compat:1ab4e1b504a35c96\"], \"migration\": {\"kind\": \"clasp-native-migration-v1\", \"strategy\": \"exact-interface-only\", \"stateType\": null, \"snapshotSymbol\": null, \"handoffSymbol\": null}}, \"decls\": [{\"kind\": \"global\", \"name\": \"hello\", \"body\": {\"kind\": \"string\", \"value\": \"Hello from Clasp\"}}, {\"kind\": \"function\", \"name\": \"id\", \"params\": [\"value\"], \"body\": {\"kind\": \"local\", \"name\": \"value\"}}, {\"kind\": \"global\", \"name\": \"main\", \"body\": {\"kind\": \"call\", \"callee\": {\"kind\": \"local\", \"name\": \"id\"}, \"args\": [{\"kind\": \"local\", \"name\": \"hello\"}]}}]}","candidateCheckOutput":"hello : Str\nid : Str -> Str\nmain : Str","candidateExplainOutput":"hello : Str\nid : Str -> Str\nmain : Str\n\nconst hello = literal:Hello from Clasp\nfunction id(value) = name:value\nconst main = call id(name:hello)","candidateNativeOutput":"format clasp-native-ir-v1\nmodule Main\nexports [hello, id, main]\nentrypoints [hello{symbol=clasp_native__Main__hello, kind=global, arity=0}, id{symbol=clasp_native__Main__id, kind=function, arity=1}, main{symbol=clasp_native__Main__main, kind=global, arity=0}]\n\nabi {\n  version clasp-native-v1\n  word_bytes 8\n  memory_strategy reference_counting\n  allocation { immediate = stack, handle = heap, global = static }\n  ownership_rules [caller_owns_returns, callee_borrows_arguments, aggregates_retain_handle_fields, globals_are_static_roots]\n  root_discovery_rules [static_globals, stack_handle_slots, heap_object_layouts]\n  lifetime_invariants [layout_and_retain_headers, declared_root_offsets_only, release_children_before_free, borrowed_handles_do_not_retain]\n  builtin_layout Int { storage = immediate, words = 1 }\n  builtin_layout Bool { storage = immediate, words = 1 }\n  builtin_layout Str { storage = handle, words = 1 }\n  builtin_layout List { storage = handle, words = 1 }\n  builtin_layout Page { storage = handle, words = 1 }\n  builtin_layout Redirect { storage = handle, words = 1 }\n  builtin_layout View { storage = handle, words = 1 }\n  builtin_layout Prompt { storage = handle, words = 1 }\n  record_layout Principal { words = 1, fields = [id:Str@word0/handle] }\n  record_layout Tenant { words = 1, fields = [id:Str@word0/handle] }\n  record_layout ResourceIdentity { words = 2, fields = [resourceType:Str@word0/handle, resourceId:Str@word1/handle] }\n  record_layout AuditActor { words = 2, fields = [actorType:Str@word0/handle, actorId:Str@word1/handle] }\n  record_layout AuditAction { words = 2, fields = [actionType:Str@word0/handle, summary:Str@word1/handle] }\n  record_layout AuditProvenance { words = 3, fields = [source:Str@word0/handle, requestId:Str@word1/handle, traceId:Str@word2/handle] }\n  record_layout SqliteConnection { words = 4, fields = [id:Str@word0/handle, databasePath:Str@word1/handle, readOnly:Bool@word2/immediate, memory:Bool@word3/immediate] }\n  record_layout AuthSession { words = 4, fields = [sessionId:Str@word0/handle, principal:Principal@word1/handle, tenant:Tenant@word2/handle, resource:ResourceIdentity@word3/handle] }\n  record_layout StandardAuditEnvelope { words = 5, fields = [actor:AuditActor@word0/handle, resource:ResourceIdentity@word1/handle, action:AuditAction@word2/handle, timestamp:Int@word3/immediate, provenance:AuditProvenance@word4/handle] }\n  object_layout Principal { kind = record, header_words = 2, words = 3, roots = [2] }\n  object_layout Tenant { kind = record, header_words = 2, words = 3, roots = [2] }\n  object_layout ResourceIdentity { kind = record, header_words = 2, words = 4, roots = [2, 3] }\n  object_layout AuditActor { kind = record, header_words = 2, words = 4, roots = [2, 3] }\n  object_layout AuditAction { kind = record, header_words = 2, words = 4, roots = [2, 3] }\n  object_layout AuditProvenance { kind = record, header_words = 2, words = 5, roots = [2, 3, 4] }\n  object_layout SqliteConnection { kind = record, header_words = 2, words = 6, roots = [2, 3] }\n  object_layout AuthSession { kind = record, header_words = 2, words = 6, roots = [2, 3, 4, 5] }\n  object_layout StandardAuditEnvelope { kind = record, header_words = 2, words = 7, roots = [2, 3, 4, 6] }\n}\n\nruntime {\n  profile compiler_backend_minimal\n  artifacts [\"runtime/clasp_runtime.h\", \"runtime/clasp_runtime.rs\"]\n  memory_symbols [clasp_rt_init, clasp_rt_register_static_root, clasp_rt_alloc_object, clasp_rt_retain, clasp_rt_release, clasp_rt_string_from_utf8, clasp_rt_bytes_new, clasp_rt_string_list_new, clasp_rt_result_ok_string, clasp_rt_result_err_string, clasp_rt_json_from_string, clasp_rt_json_to_string, clasp_rt_binary_from_json, clasp_rt_json_from_binary, clasp_rt_transport_frame, clasp_rt_transport_unframe]\n  bindings []\n  json_codecs []\n  binary_codecs []\n  boundaries []\n  service_transports []\n}\n\nglobal hello = string(\"Hello from Clasp\")\n\nfunction id(value) = local(value)\n\nglobal main = call(local(id), [local(hello)])\n\n","emittedHello":"Hello from Clasp","emittedIdentity":"Ada","emittedMain":"Hello from Clasp","parsedSampleModuleName":"Main","parsedSampleImports":["Compiler.Lower"],"parsedSampleDeclNames":["hello","id","main"],"parsedSampleMainExpr":"call id(hello)","secondaryParsedModuleName":"Secondary","secondaryParsedDeclNames":["salute","echo","main"],"secondaryCheckedModule":"salute : Str\necho : Str -> Str\nmain : Str","secondaryLoweredModule":"const salute = literal:Hi\nfunction echo(value) = name:value\nconst main = call echo(name:salute)","secondaryEmittedModule":"// Generated by compiler-selfhost\nfunction $claspSnapshotValue(value) {\n  if (typeof globalThis.structuredClone === \"function\") {\n    return globalThis.structuredClone(value);\n  }\n  return value === undefined ? undefined : JSON.parse(JSON.stringify(value));\n}\n\nfunction $claspExpectObject(value, path = \"value\") {\n  if (typeof value !== \"object\" || value === null || Array.isArray(value)) {\n    throw new Error(`${path} expected an object`);\n  }\n  return value;\n}\n\nfunction $claspExpectArray(value, path = \"value\") {\n  if (!Array.isArray(value)) {\n    throw new Error(`${path} expected an array`);\n  }\n  return value;\n}\n\nfunction $claspExpectString(value, path = \"value\") {\n  if (typeof value !== \"string\") {\n    throw new Error(`${path} expected a string`);\n  }\n  return value;\n}\n\nfunction $claspExpectInt(value, path = \"value\") {\n  if (!Number.isInteger(value)) {\n    throw new Error(`${path} expected an integer`);\n  }\n  return value;\n}\n\nfunction $claspExpectBool(value, path = \"value\") {\n  if (typeof value !== \"boolean\") {\n    throw new Error(`${path} expected a boolean`);\n  }\n  return value;\n}\n\nconst $claspBinaryMagic = Object.freeze([67, 76, 66, 49]);\nconst $claspTextEncoder = typeof TextEncoder === \"undefined\" ? null : new TextEncoder();\nconst $claspTextDecoder = typeof TextDecoder === \"undefined\" ? null : new TextDecoder();\n\nfunction $claspEncodeUtf8Text(value) {\n  if ($claspTextEncoder === null) {\n    throw new Error(\"This runtime does not provide TextEncoder for binary Clasp codecs\");\n  }\n  return $claspTextEncoder.encode($claspExpectString(value, \"value\"));\n}\n\nfunction $claspDecodeUtf8Text(bytes, path = \"value\") {\n  if ($claspTextDecoder === null) {\n    throw new Error(\"This runtime does not provide TextDecoder for binary Clasp codecs\");\n  }\n  return $claspTextDecoder.decode($claspNormalizeBinaryBytes(bytes, path));\n}\n\nfunction $claspNormalizeBinaryBytes(value, path = \"value\") {\n  if (value instanceof Uint8Array) {\n    return value;\n  }\n  if (value instanceof ArrayBuffer) {\n    return new Uint8Array(value);\n  }\n  if (ArrayBuffer.isView(value)) {\n    return new Uint8Array(value.buffer, value.byteOffset, value.byteLength);\n  }\n  throw new Error(`${path} expected binary bytes`);\n}\n\nfunction $claspWriteUint32(target, offset, value) {\n  new DataView(target.buffer, target.byteOffset, target.byteLength).setUint32(offset, value, true);\n}\n\nfunction $claspReadUint32(source, offset, path = \"value\") {\n  if (offset + 4 > source.byteLength) {\n    throw new Error(`${path} truncated binary payload`);\n  }\n  return new DataView(source.buffer, source.byteOffset, source.byteLength).getUint32(offset, true);\n}\n\nfunction $claspFrameBinaryPayload(payload) {\n  const bytes = $claspNormalizeBinaryBytes(payload, \"payload\");\n  const frame = new Uint8Array(4 + bytes.byteLength);\n  $claspWriteUint32(frame, 0, bytes.byteLength);\n  frame.set(bytes, 4);\n  return frame;\n}\n\nfunction $claspUnframeBinaryPayload(frame, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(frame, path);\n  const expectedLength = $claspReadUint32(bytes, 0, path);\n  const payload = bytes.slice(4);\n  if (payload.byteLength !== expectedLength) {\n    throw new Error(`${path} expected ${expectedLength} framed bytes but received ${payload.byteLength}`);\n  }\n  return payload;\n}\n\nfunction $claspEncodeSchemaBinaryPayload(schemaContract, value) {\n  const normalizedValue = schemaContract.toHost(schemaContract.fromHost(value, \"value\"), \"value\");\n  const fingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  const fingerprintBytes = $claspEncodeUtf8Text(fingerprint);\n  const payloadBytes = $claspEncodeUtf8Text(JSON.stringify(normalizedValue));\n  const buffer = new Uint8Array(12 + fingerprintBytes.byteLength + payloadBytes.byteLength);\n  buffer.set($claspBinaryMagic, 0);\n  $claspWriteUint32(buffer, 4, fingerprintBytes.byteLength);\n  $claspWriteUint32(buffer, 8, payloadBytes.byteLength);\n  buffer.set(fingerprintBytes, 12);\n  buffer.set(payloadBytes, 12 + fingerprintBytes.byteLength);\n  return buffer;\n}\n\nfunction $claspDecodeSchemaBinaryPayload(schemaContract, payload, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(payload, path);\n  if (bytes.byteLength < 12) {\n    throw new Error(`${path} expected a Clasp binary payload`);\n  }\n  for (let index = 0; index < $claspBinaryMagic.length; index += 1) {\n    if (bytes[index] !== $claspBinaryMagic[index]) {\n      throw new Error(`${path} expected a Clasp binary payload`);\n    }\n  }\n  const fingerprintLength = $claspReadUint32(bytes, 4, path);\n  const payloadLength = $claspReadUint32(bytes, 8, path);\n  const payloadOffset = 12 + fingerprintLength;\n  if (payloadOffset + payloadLength !== bytes.byteLength) {\n    throw new Error(`${path} has an invalid Clasp binary payload length`);\n  }\n  const fingerprint = $claspDecodeUtf8Text(bytes.slice(12, payloadOffset), `${path}.fingerprint`);\n  const expectedFingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  if (fingerprint !== expectedFingerprint) {\n    throw new Error(`${path} expected schema fingerprint ${expectedFingerprint} but received ${fingerprint}`);\n  }\n  const payloadText = $claspDecodeUtf8Text(bytes.slice(payloadOffset), `${path}.payload`);\n  return schemaContract.toHost(schemaContract.fromHost(JSON.parse(payloadText), path), path);\n}\n\nfunction $claspCreateSchemaBinaryCodec(schemaContract) {\n  return Object.freeze({\n    type: schemaContract.type ?? schemaContract.name ?? null,\n    schemaFingerprint: String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\"),\n    framing: \"length_prefixed\",\n    encode(value) { return $claspEncodeSchemaBinaryPayload(schemaContract, value); },\n    decode(bytes, path = \"value\") { return $claspDecodeSchemaBinaryPayload(schemaContract, bytes, path); },\n    frame(value) { return $claspFrameBinaryPayload(this.encode(value)); },\n    unframe(frame, path = \"value\") { return this.decode($claspUnframeBinaryPayload(frame, path), path); }\n  });\n}\n\nfunction $claspCreateSchemaStream(contract, initial = null) {\n  return Object.freeze({ kind: \"clasp-schema-stream\", schema: contract, initial });\n}\n\nfunction $claspPromptMessage(role, content) {\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([Object.freeze({\n      role: $claspExpectString(role, \"prompt.role\"),\n      content: $claspExpectString(content, \"prompt.content\")\n    })])\n  });\n}\n\nfunction $claspExpectPrompt(value, path = \"value\") {\n  const objectValue = $claspExpectObject(value, path);\n  if (objectValue.$kind !== \"prompt\") {\n    throw new Error(`${path} expected a Prompt value`);\n  }\n  const messages = $claspExpectArray(objectValue.messages, `${path}.messages`).map((message, index) => {\n    const messagePath = `${path}.messages[${index}]`;\n    const messageObject = $claspExpectObject(message, messagePath);\n    return Object.freeze({\n      role: $claspExpectString(messageObject.role, `${messagePath}.role`),\n      content: $claspExpectString(messageObject.content, `${messagePath}.content`)\n    });\n  });\n  return Object.freeze({ $kind: \"prompt\", messages: Object.freeze(messages) });\n}\n\nfunction systemPrompt(content) { return $claspPromptMessage(\"system\", content); }\nfunction assistantPrompt(content) { return $claspPromptMessage(\"assistant\", content); }\nfunction userPrompt(content) { return $claspPromptMessage(\"user\", content); }\n\nfunction appendPrompt(left, right) {\n  const leftPrompt = $claspExpectPrompt(left, \"left\");\n  const rightPrompt = $claspExpectPrompt(right, \"right\");\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([...leftPrompt.messages, ...rightPrompt.messages].map((message) => Object.freeze({ role: message.role, content: message.content })))\n  });\n}\n\nfunction promptText(value) {\n  const prompt = $claspExpectPrompt(value, \"value\");\n  return prompt.messages.map((message) => `${message.role}: ${message.content}`).join(\"\\n\\n\");\n}\n\nfunction $claspCreateTraceCollector(seed = []) {\n  const entries = Array.isArray(seed) ? seed.map((entry) => $claspSnapshotValue(entry)) : [];\n  return Object.freeze({\n    kind: \"clasp-trace-collector\",\n    version: 1,\n    record(entry) {\n      const snapshot = $claspSnapshotValue(entry);\n      entries.push(snapshot);\n      return snapshot;\n    },\n    entries() {\n      return $claspSnapshotValue(entries);\n    },\n    clear() {\n      entries.length = 0;\n      return this.entries();\n    }\n  });\n}\n\nfunction $claspMaybeRecordTrace(options = null, kind, name, action) {\n  const collector = options && typeof options === \"object\" && options.collector && typeof options.collector.record === \"function\"\n    ? options.collector\n    : null;\n  if (collector) {\n    collector.record(Object.freeze({ kind, name, action, status: \"ok\" }));\n  }\n}\nexport const salute = \"Hi\";\nexport function echo(value) { return value; }\nexport const main = echo(salute);\nconst __claspExports = Object.freeze({ salute: salute, echo: echo, main: main });\n\nexport const __claspSchemas = Object.freeze({  });\nfor (const schemaContract of Object.values(__claspSchemas)) {\n  schemaContract.binary = $claspCreateSchemaBinaryCodec(schemaContract);\n  schemaContract.encodeBinary = function (value) { return this.binary.encode(value); };\n  schemaContract.decodeBinary = function (payload, path = \"value\") { return this.binary.decode(payload, path); };\n  schemaContract.encodeFramedBinary = function (value) { return this.binary.frame(value); };\n  schemaContract.decodeFramedBinary = function (frame, path = \"value\") { return this.binary.unframe(frame, path); };\n  Object.freeze(schemaContract);\n}\n\nexport const __claspTraceCollector = Object.freeze({\n  version: 1,\n  create(seed = []) { return $claspCreateTraceCollector(seed); }\n});\n\nexport const __claspModule = Object.freeze({\n  name: \"Secondary\",\n  version: 1\n});\n\nexport const __claspBindings = Object.freeze({\n  kind: \"clasp-generated-bindings\",\n  version: 1,\n  module: __claspModule,\n  schemas: __claspSchemas,\n  traces: __claspTraceCollector\n});\n","tertiaryParsedModuleName":"Collections","tertiaryParsedDeclNames":["flags","scores","main"],"tertiaryCheckedModule":"flags : [Bool]\nscores : [Int]\nmain : [Int]","tertiaryLoweredModule":"const flags = list:[bool:true, bool:false]\nconst scores = list:[int:7, int:9]\nconst main = name:scores","tertiaryEmittedModule":"// Generated by compiler-selfhost\nfunction $claspSnapshotValue(value) {\n  if (typeof globalThis.structuredClone === \"function\") {\n    return globalThis.structuredClone(value);\n  }\n  return value === undefined ? undefined : JSON.parse(JSON.stringify(value));\n}\n\nfunction $claspExpectObject(value, path = \"value\") {\n  if (typeof value !== \"object\" || value === null || Array.isArray(value)) {\n    throw new Error(`${path} expected an object`);\n  }\n  return value;\n}\n\nfunction $claspExpectArray(value, path = \"value\") {\n  if (!Array.isArray(value)) {\n    throw new Error(`${path} expected an array`);\n  }\n  return value;\n}\n\nfunction $claspExpectString(value, path = \"value\") {\n  if (typeof value !== \"string\") {\n    throw new Error(`${path} expected a string`);\n  }\n  return value;\n}\n\nfunction $claspExpectInt(value, path = \"value\") {\n  if (!Number.isInteger(value)) {\n    throw new Error(`${path} expected an integer`);\n  }\n  return value;\n}\n\nfunction $claspExpectBool(value, path = \"value\") {\n  if (typeof value !== \"boolean\") {\n    throw new Error(`${path} expected a boolean`);\n  }\n  return value;\n}\n\nconst $claspBinaryMagic = Object.freeze([67, 76, 66, 49]);\nconst $claspTextEncoder = typeof TextEncoder === \"undefined\" ? null : new TextEncoder();\nconst $claspTextDecoder = typeof TextDecoder === \"undefined\" ? null : new TextDecoder();\n\nfunction $claspEncodeUtf8Text(value) {\n  if ($claspTextEncoder === null) {\n    throw new Error(\"This runtime does not provide TextEncoder for binary Clasp codecs\");\n  }\n  return $claspTextEncoder.encode($claspExpectString(value, \"value\"));\n}\n\nfunction $claspDecodeUtf8Text(bytes, path = \"value\") {\n  if ($claspTextDecoder === null) {\n    throw new Error(\"This runtime does not provide TextDecoder for binary Clasp codecs\");\n  }\n  return $claspTextDecoder.decode($claspNormalizeBinaryBytes(bytes, path));\n}\n\nfunction $claspNormalizeBinaryBytes(value, path = \"value\") {\n  if (value instanceof Uint8Array) {\n    return value;\n  }\n  if (value instanceof ArrayBuffer) {\n    return new Uint8Array(value);\n  }\n  if (ArrayBuffer.isView(value)) {\n    return new Uint8Array(value.buffer, value.byteOffset, value.byteLength);\n  }\n  throw new Error(`${path} expected binary bytes`);\n}\n\nfunction $claspWriteUint32(target, offset, value) {\n  new DataView(target.buffer, target.byteOffset, target.byteLength).setUint32(offset, value, true);\n}\n\nfunction $claspReadUint32(source, offset, path = \"value\") {\n  if (offset + 4 > source.byteLength) {\n    throw new Error(`${path} truncated binary payload`);\n  }\n  return new DataView(source.buffer, source.byteOffset, source.byteLength).getUint32(offset, true);\n}\n\nfunction $claspFrameBinaryPayload(payload) {\n  const bytes = $claspNormalizeBinaryBytes(payload, \"payload\");\n  const frame = new Uint8Array(4 + bytes.byteLength);\n  $claspWriteUint32(frame, 0, bytes.byteLength);\n  frame.set(bytes, 4);\n  return frame;\n}\n\nfunction $claspUnframeBinaryPayload(frame, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(frame, path);\n  const expectedLength = $claspReadUint32(bytes, 0, path);\n  const payload = bytes.slice(4);\n  if (payload.byteLength !== expectedLength) {\n    throw new Error(`${path} expected ${expectedLength} framed bytes but received ${payload.byteLength}`);\n  }\n  return payload;\n}\n\nfunction $claspEncodeSchemaBinaryPayload(schemaContract, value) {\n  const normalizedValue = schemaContract.toHost(schemaContract.fromHost(value, \"value\"), \"value\");\n  const fingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  const fingerprintBytes = $claspEncodeUtf8Text(fingerprint);\n  const payloadBytes = $claspEncodeUtf8Text(JSON.stringify(normalizedValue));\n  const buffer = new Uint8Array(12 + fingerprintBytes.byteLength + payloadBytes.byteLength);\n  buffer.set($claspBinaryMagic, 0);\n  $claspWriteUint32(buffer, 4, fingerprintBytes.byteLength);\n  $claspWriteUint32(buffer, 8, payloadBytes.byteLength);\n  buffer.set(fingerprintBytes, 12);\n  buffer.set(payloadBytes, 12 + fingerprintBytes.byteLength);\n  return buffer;\n}\n\nfunction $claspDecodeSchemaBinaryPayload(schemaContract, payload, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(payload, path);\n  if (bytes.byteLength < 12) {\n    throw new Error(`${path} expected a Clasp binary payload`);\n  }\n  for (let index = 0; index < $claspBinaryMagic.length; index += 1) {\n    if (bytes[index] !== $claspBinaryMagic[index]) {\n      throw new Error(`${path} expected a Clasp binary payload`);\n    }\n  }\n  const fingerprintLength = $claspReadUint32(bytes, 4, path);\n  const payloadLength = $claspReadUint32(bytes, 8, path);\n  const payloadOffset = 12 + fingerprintLength;\n  if (payloadOffset + payloadLength !== bytes.byteLength) {\n    throw new Error(`${path} has an invalid Clasp binary payload length`);\n  }\n  const fingerprint = $claspDecodeUtf8Text(bytes.slice(12, payloadOffset), `${path}.fingerprint`);\n  const expectedFingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  if (fingerprint !== expectedFingerprint) {\n    throw new Error(`${path} expected schema fingerprint ${expectedFingerprint} but received ${fingerprint}`);\n  }\n  const payloadText = $claspDecodeUtf8Text(bytes.slice(payloadOffset), `${path}.payload`);\n  return schemaContract.toHost(schemaContract.fromHost(JSON.parse(payloadText), path), path);\n}\n\nfunction $claspCreateSchemaBinaryCodec(schemaContract) {\n  return Object.freeze({\n    type: schemaContract.type ?? schemaContract.name ?? null,\n    schemaFingerprint: String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\"),\n    framing: \"length_prefixed\",\n    encode(value) { return $claspEncodeSchemaBinaryPayload(schemaContract, value); },\n    decode(bytes, path = \"value\") { return $claspDecodeSchemaBinaryPayload(schemaContract, bytes, path); },\n    frame(value) { return $claspFrameBinaryPayload(this.encode(value)); },\n    unframe(frame, path = \"value\") { return this.decode($claspUnframeBinaryPayload(frame, path), path); }\n  });\n}\n\nfunction $claspCreateSchemaStream(contract, initial = null) {\n  return Object.freeze({ kind: \"clasp-schema-stream\", schema: contract, initial });\n}\n\nfunction $claspPromptMessage(role, content) {\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([Object.freeze({\n      role: $claspExpectString(role, \"prompt.role\"),\n      content: $claspExpectString(content, \"prompt.content\")\n    })])\n  });\n}\n\nfunction $claspExpectPrompt(value, path = \"value\") {\n  const objectValue = $claspExpectObject(value, path);\n  if (objectValue.$kind !== \"prompt\") {\n    throw new Error(`${path} expected a Prompt value`);\n  }\n  const messages = $claspExpectArray(objectValue.messages, `${path}.messages`).map((message, index) => {\n    const messagePath = `${path}.messages[${index}]`;\n    const messageObject = $claspExpectObject(message, messagePath);\n    return Object.freeze({\n      role: $claspExpectString(messageObject.role, `${messagePath}.role`),\n      content: $claspExpectString(messageObject.content, `${messagePath}.content`)\n    });\n  });\n  return Object.freeze({ $kind: \"prompt\", messages: Object.freeze(messages) });\n}\n\nfunction systemPrompt(content) { return $claspPromptMessage(\"system\", content); }\nfunction assistantPrompt(content) { return $claspPromptMessage(\"assistant\", content); }\nfunction userPrompt(content) { return $claspPromptMessage(\"user\", content); }\n\nfunction appendPrompt(left, right) {\n  const leftPrompt = $claspExpectPrompt(left, \"left\");\n  const rightPrompt = $claspExpectPrompt(right, \"right\");\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([...leftPrompt.messages, ...rightPrompt.messages].map((message) => Object.freeze({ role: message.role, content: message.content })))\n  });\n}\n\nfunction promptText(value) {\n  const prompt = $claspExpectPrompt(value, \"value\");\n  return prompt.messages.map((message) => `${message.role}: ${message.content}`).join(\"\\n\\n\");\n}\n\nfunction $claspCreateTraceCollector(seed = []) {\n  const entries = Array.isArray(seed) ? seed.map((entry) => $claspSnapshotValue(entry)) : [];\n  return Object.freeze({\n    kind: \"clasp-trace-collector\",\n    version: 1,\n    record(entry) {\n      const snapshot = $claspSnapshotValue(entry);\n      entries.push(snapshot);\n      return snapshot;\n    },\n    entries() {\n      return $claspSnapshotValue(entries);\n    },\n    clear() {\n      entries.length = 0;\n      return this.entries();\n    }\n  });\n}\n\nfunction $claspMaybeRecordTrace(options = null, kind, name, action) {\n  const collector = options && typeof options === \"object\" && options.collector && typeof options.collector.record === \"function\"\n    ? options.collector\n    : null;\n  if (collector) {\n    collector.record(Object.freeze({ kind, name, action, status: \"ok\" }));\n  }\n}\nexport const flags = [true, false];\nexport const scores = [7, 9];\nexport const main = scores;\nconst __claspExports = Object.freeze({ flags: flags, scores: scores, main: main });\n\nexport const __claspSchemas = Object.freeze({  });\nfor (const schemaContract of Object.values(__claspSchemas)) {\n  schemaContract.binary = $claspCreateSchemaBinaryCodec(schemaContract);\n  schemaContract.encodeBinary = function (value) { return this.binary.encode(value); };\n  schemaContract.decodeBinary = function (payload, path = \"value\") { return this.binary.decode(payload, path); };\n  schemaContract.encodeFramedBinary = function (value) { return this.binary.frame(value); };\n  schemaContract.decodeFramedBinary = function (frame, path = \"value\") { return this.binary.unframe(frame, path); };\n  Object.freeze(schemaContract);\n}\n\nexport const __claspTraceCollector = Object.freeze({\n  version: 1,\n  create(seed = []) { return $claspCreateTraceCollector(seed); }\n});\n\nexport const __claspModule = Object.freeze({\n  name: \"Collections\",\n  version: 1\n});\n\nexport const __claspBindings = Object.freeze({\n  kind: \"clasp-generated-bindings\",\n  version: 1,\n  module: __claspModule,\n  schemas: __claspSchemas,\n  traces: __claspTraceCollector\n});\n","quaternaryParsedModuleName":"Pairing","quaternaryParsedDeclNames":["select","wrap","labels","main"],"quaternaryCheckedModule":"select : Str -> Str -> Str\nwrap : Str -> Str\nlabels : [Str]\nmain : Str","quaternaryLoweredModule":"function select(left, right) = name:right\nfunction wrap(value) = name:value\nconst labels = list:[call wrap(call select(literal:left, literal:right)), literal:done]\nconst main = call wrap(call select(literal:alpha, literal:beta))","quaternaryEmittedModule":"// Generated by compiler-selfhost\nfunction $claspSnapshotValue(value) {\n  if (typeof globalThis.structuredClone === \"function\") {\n    return globalThis.structuredClone(value);\n  }\n  return value === undefined ? undefined : JSON.parse(JSON.stringify(value));\n}\n\nfunction $claspExpectObject(value, path = \"value\") {\n  if (typeof value !== \"object\" || value === null || Array.isArray(value)) {\n    throw new Error(`${path} expected an object`);\n  }\n  return value;\n}\n\nfunction $claspExpectArray(value, path = \"value\") {\n  if (!Array.isArray(value)) {\n    throw new Error(`${path} expected an array`);\n  }\n  return value;\n}\n\nfunction $claspExpectString(value, path = \"value\") {\n  if (typeof value !== \"string\") {\n    throw new Error(`${path} expected a string`);\n  }\n  return value;\n}\n\nfunction $claspExpectInt(value, path = \"value\") {\n  if (!Number.isInteger(value)) {\n    throw new Error(`${path} expected an integer`);\n  }\n  return value;\n}\n\nfunction $claspExpectBool(value, path = \"value\") {\n  if (typeof value !== \"boolean\") {\n    throw new Error(`${path} expected a boolean`);\n  }\n  return value;\n}\n\nconst $claspBinaryMagic = Object.freeze([67, 76, 66, 49]);\nconst $claspTextEncoder = typeof TextEncoder === \"undefined\" ? null : new TextEncoder();\nconst $claspTextDecoder = typeof TextDecoder === \"undefined\" ? null : new TextDecoder();\n\nfunction $claspEncodeUtf8Text(value) {\n  if ($claspTextEncoder === null) {\n    throw new Error(\"This runtime does not provide TextEncoder for binary Clasp codecs\");\n  }\n  return $claspTextEncoder.encode($claspExpectString(value, \"value\"));\n}\n\nfunction $claspDecodeUtf8Text(bytes, path = \"value\") {\n  if ($claspTextDecoder === null) {\n    throw new Error(\"This runtime does not provide TextDecoder for binary Clasp codecs\");\n  }\n  return $claspTextDecoder.decode($claspNormalizeBinaryBytes(bytes, path));\n}\n\nfunction $claspNormalizeBinaryBytes(value, path = \"value\") {\n  if (value instanceof Uint8Array) {\n    return value;\n  }\n  if (value instanceof ArrayBuffer) {\n    return new Uint8Array(value);\n  }\n  if (ArrayBuffer.isView(value)) {\n    return new Uint8Array(value.buffer, value.byteOffset, value.byteLength);\n  }\n  throw new Error(`${path} expected binary bytes`);\n}\n\nfunction $claspWriteUint32(target, offset, value) {\n  new DataView(target.buffer, target.byteOffset, target.byteLength).setUint32(offset, value, true);\n}\n\nfunction $claspReadUint32(source, offset, path = \"value\") {\n  if (offset + 4 > source.byteLength) {\n    throw new Error(`${path} truncated binary payload`);\n  }\n  return new DataView(source.buffer, source.byteOffset, source.byteLength).getUint32(offset, true);\n}\n\nfunction $claspFrameBinaryPayload(payload) {\n  const bytes = $claspNormalizeBinaryBytes(payload, \"payload\");\n  const frame = new Uint8Array(4 + bytes.byteLength);\n  $claspWriteUint32(frame, 0, bytes.byteLength);\n  frame.set(bytes, 4);\n  return frame;\n}\n\nfunction $claspUnframeBinaryPayload(frame, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(frame, path);\n  const expectedLength = $claspReadUint32(bytes, 0, path);\n  const payload = bytes.slice(4);\n  if (payload.byteLength !== expectedLength) {\n    throw new Error(`${path} expected ${expectedLength} framed bytes but received ${payload.byteLength}`);\n  }\n  return payload;\n}\n\nfunction $claspEncodeSchemaBinaryPayload(schemaContract, value) {\n  const normalizedValue = schemaContract.toHost(schemaContract.fromHost(value, \"value\"), \"value\");\n  const fingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  const fingerprintBytes = $claspEncodeUtf8Text(fingerprint);\n  const payloadBytes = $claspEncodeUtf8Text(JSON.stringify(normalizedValue));\n  const buffer = new Uint8Array(12 + fingerprintBytes.byteLength + payloadBytes.byteLength);\n  buffer.set($claspBinaryMagic, 0);\n  $claspWriteUint32(buffer, 4, fingerprintBytes.byteLength);\n  $claspWriteUint32(buffer, 8, payloadBytes.byteLength);\n  buffer.set(fingerprintBytes, 12);\n  buffer.set(payloadBytes, 12 + fingerprintBytes.byteLength);\n  return buffer;\n}\n\nfunction $claspDecodeSchemaBinaryPayload(schemaContract, payload, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(payload, path);\n  if (bytes.byteLength < 12) {\n    throw new Error(`${path} expected a Clasp binary payload`);\n  }\n  for (let index = 0; index < $claspBinaryMagic.length; index += 1) {\n    if (bytes[index] !== $claspBinaryMagic[index]) {\n      throw new Error(`${path} expected a Clasp binary payload`);\n    }\n  }\n  const fingerprintLength = $claspReadUint32(bytes, 4, path);\n  const payloadLength = $claspReadUint32(bytes, 8, path);\n  const payloadOffset = 12 + fingerprintLength;\n  if (payloadOffset + payloadLength !== bytes.byteLength) {\n    throw new Error(`${path} has an invalid Clasp binary payload length`);\n  }\n  const fingerprint = $claspDecodeUtf8Text(bytes.slice(12, payloadOffset), `${path}.fingerprint`);\n  const expectedFingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  if (fingerprint !== expectedFingerprint) {\n    throw new Error(`${path} expected schema fingerprint ${expectedFingerprint} but received ${fingerprint}`);\n  }\n  const payloadText = $claspDecodeUtf8Text(bytes.slice(payloadOffset), `${path}.payload`);\n  return schemaContract.toHost(schemaContract.fromHost(JSON.parse(payloadText), path), path);\n}\n\nfunction $claspCreateSchemaBinaryCodec(schemaContract) {\n  return Object.freeze({\n    type: schemaContract.type ?? schemaContract.name ?? null,\n    schemaFingerprint: String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\"),\n    framing: \"length_prefixed\",\n    encode(value) { return $claspEncodeSchemaBinaryPayload(schemaContract, value); },\n    decode(bytes, path = \"value\") { return $claspDecodeSchemaBinaryPayload(schemaContract, bytes, path); },\n    frame(value) { return $claspFrameBinaryPayload(this.encode(value)); },\n    unframe(frame, path = \"value\") { return this.decode($claspUnframeBinaryPayload(frame, path), path); }\n  });\n}\n\nfunction $claspCreateSchemaStream(contract, initial = null) {\n  return Object.freeze({ kind: \"clasp-schema-stream\", schema: contract, initial });\n}\n\nfunction $claspPromptMessage(role, content) {\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([Object.freeze({\n      role: $claspExpectString(role, \"prompt.role\"),\n      content: $claspExpectString(content, \"prompt.content\")\n    })])\n  });\n}\n\nfunction $claspExpectPrompt(value, path = \"value\") {\n  const objectValue = $claspExpectObject(value, path);\n  if (objectValue.$kind !== \"prompt\") {\n    throw new Error(`${path} expected a Prompt value`);\n  }\n  const messages = $claspExpectArray(objectValue.messages, `${path}.messages`).map((message, index) => {\n    const messagePath = `${path}.messages[${index}]`;\n    const messageObject = $claspExpectObject(message, messagePath);\n    return Object.freeze({\n      role: $claspExpectString(messageObject.role, `${messagePath}.role`),\n      content: $claspExpectString(messageObject.content, `${messagePath}.content`)\n    });\n  });\n  return Object.freeze({ $kind: \"prompt\", messages: Object.freeze(messages) });\n}\n\nfunction systemPrompt(content) { return $claspPromptMessage(\"system\", content); }\nfunction assistantPrompt(content) { return $claspPromptMessage(\"assistant\", content); }\nfunction userPrompt(content) { return $claspPromptMessage(\"user\", content); }\n\nfunction appendPrompt(left, right) {\n  const leftPrompt = $claspExpectPrompt(left, \"left\");\n  const rightPrompt = $claspExpectPrompt(right, \"right\");\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([...leftPrompt.messages, ...rightPrompt.messages].map((message) => Object.freeze({ role: message.role, content: message.content })))\n  });\n}\n\nfunction promptText(value) {\n  const prompt = $claspExpectPrompt(value, \"value\");\n  return prompt.messages.map((message) => `${message.role}: ${message.content}`).join(\"\\n\\n\");\n}\n\nfunction $claspCreateTraceCollector(seed = []) {\n  const entries = Array.isArray(seed) ? seed.map((entry) => $claspSnapshotValue(entry)) : [];\n  return Object.freeze({\n    kind: \"clasp-trace-collector\",\n    version: 1,\n    record(entry) {\n      const snapshot = $claspSnapshotValue(entry);\n      entries.push(snapshot);\n      return snapshot;\n    },\n    entries() {\n      return $claspSnapshotValue(entries);\n    },\n    clear() {\n      entries.length = 0;\n      return this.entries();\n    }\n  });\n}\n\nfunction $claspMaybeRecordTrace(options = null, kind, name, action) {\n  const collector = options && typeof options === \"object\" && options.collector && typeof options.collector.record === \"function\"\n    ? options.collector\n    : null;\n  if (collector) {\n    collector.record(Object.freeze({ kind, name, action, status: \"ok\" }));\n  }\n}\nexport function select(left, right) { return right; }\nexport function wrap(value) { return value; }\nexport const labels = [wrap(select(\"left\", \"right\")), \"done\"];\nexport const main = wrap(select(\"alpha\", \"beta\"));\nconst __claspExports = Object.freeze({ select: select, wrap: wrap, labels: labels, main: main });\n\nexport const __claspSchemas = Object.freeze({  });\nfor (const schemaContract of Object.values(__claspSchemas)) {\n  schemaContract.binary = $claspCreateSchemaBinaryCodec(schemaContract);\n  schemaContract.encodeBinary = function (value) { return this.binary.encode(value); };\n  schemaContract.decodeBinary = function (payload, path = \"value\") { return this.binary.decode(payload, path); };\n  schemaContract.encodeFramedBinary = function (value) { return this.binary.frame(value); };\n  schemaContract.decodeFramedBinary = function (frame, path = \"value\") { return this.binary.unframe(frame, path); };\n  Object.freeze(schemaContract);\n}\n\nexport const __claspTraceCollector = Object.freeze({\n  version: 1,\n  create(seed = []) { return $claspCreateTraceCollector(seed); }\n});\n\nexport const __claspModule = Object.freeze({\n  name: \"Pairing\",\n  version: 1\n});\n\nexport const __claspBindings = Object.freeze({\n  kind: \"clasp-generated-bindings\",\n  version: 1,\n  module: __claspModule,\n  schemas: __claspSchemas,\n  traces: __claspTraceCollector\n});\n","quinaryParsedModuleName":"Records","quinaryParsedRecordNames":["User"],"quinaryParsedRecordFieldTypes":["name : Str","active : Bool"],"quinaryParsedDeclNames":["defaultUser","userName","main"],"quinaryCheckedModule":"defaultUser : User\nuserName : User -> Str\nmain : Str","quinaryLoweredModule":"const defaultUser = record User {name = literal:Ada, active = bool:true}\nfunction userName(user) = field(User, name:user, name)\nconst main = call userName(name:defaultUser)","quinaryEmittedModule":"// Generated by compiler-selfhost\nfunction $claspSnapshotValue(value) {\n  if (typeof globalThis.structuredClone === \"function\") {\n    return globalThis.structuredClone(value);\n  }\n  return value === undefined ? undefined : JSON.parse(JSON.stringify(value));\n}\n\nfunction $claspExpectObject(value, path = \"value\") {\n  if (typeof value !== \"object\" || value === null || Array.isArray(value)) {\n    throw new Error(`${path} expected an object`);\n  }\n  return value;\n}\n\nfunction $claspExpectArray(value, path = \"value\") {\n  if (!Array.isArray(value)) {\n    throw new Error(`${path} expected an array`);\n  }\n  return value;\n}\n\nfunction $claspExpectString(value, path = \"value\") {\n  if (typeof value !== \"string\") {\n    throw new Error(`${path} expected a string`);\n  }\n  return value;\n}\n\nfunction $claspExpectInt(value, path = \"value\") {\n  if (!Number.isInteger(value)) {\n    throw new Error(`${path} expected an integer`);\n  }\n  return value;\n}\n\nfunction $claspExpectBool(value, path = \"value\") {\n  if (typeof value !== \"boolean\") {\n    throw new Error(`${path} expected a boolean`);\n  }\n  return value;\n}\n\nconst $claspBinaryMagic = Object.freeze([67, 76, 66, 49]);\nconst $claspTextEncoder = typeof TextEncoder === \"undefined\" ? null : new TextEncoder();\nconst $claspTextDecoder = typeof TextDecoder === \"undefined\" ? null : new TextDecoder();\n\nfunction $claspEncodeUtf8Text(value) {\n  if ($claspTextEncoder === null) {\n    throw new Error(\"This runtime does not provide TextEncoder for binary Clasp codecs\");\n  }\n  return $claspTextEncoder.encode($claspExpectString(value, \"value\"));\n}\n\nfunction $claspDecodeUtf8Text(bytes, path = \"value\") {\n  if ($claspTextDecoder === null) {\n    throw new Error(\"This runtime does not provide TextDecoder for binary Clasp codecs\");\n  }\n  return $claspTextDecoder.decode($claspNormalizeBinaryBytes(bytes, path));\n}\n\nfunction $claspNormalizeBinaryBytes(value, path = \"value\") {\n  if (value instanceof Uint8Array) {\n    return value;\n  }\n  if (value instanceof ArrayBuffer) {\n    return new Uint8Array(value);\n  }\n  if (ArrayBuffer.isView(value)) {\n    return new Uint8Array(value.buffer, value.byteOffset, value.byteLength);\n  }\n  throw new Error(`${path} expected binary bytes`);\n}\n\nfunction $claspWriteUint32(target, offset, value) {\n  new DataView(target.buffer, target.byteOffset, target.byteLength).setUint32(offset, value, true);\n}\n\nfunction $claspReadUint32(source, offset, path = \"value\") {\n  if (offset + 4 > source.byteLength) {\n    throw new Error(`${path} truncated binary payload`);\n  }\n  return new DataView(source.buffer, source.byteOffset, source.byteLength).getUint32(offset, true);\n}\n\nfunction $claspFrameBinaryPayload(payload) {\n  const bytes = $claspNormalizeBinaryBytes(payload, \"payload\");\n  const frame = new Uint8Array(4 + bytes.byteLength);\n  $claspWriteUint32(frame, 0, bytes.byteLength);\n  frame.set(bytes, 4);\n  return frame;\n}\n\nfunction $claspUnframeBinaryPayload(frame, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(frame, path);\n  const expectedLength = $claspReadUint32(bytes, 0, path);\n  const payload = bytes.slice(4);\n  if (payload.byteLength !== expectedLength) {\n    throw new Error(`${path} expected ${expectedLength} framed bytes but received ${payload.byteLength}`);\n  }\n  return payload;\n}\n\nfunction $claspEncodeSchemaBinaryPayload(schemaContract, value) {\n  const normalizedValue = schemaContract.toHost(schemaContract.fromHost(value, \"value\"), \"value\");\n  const fingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  const fingerprintBytes = $claspEncodeUtf8Text(fingerprint);\n  const payloadBytes = $claspEncodeUtf8Text(JSON.stringify(normalizedValue));\n  const buffer = new Uint8Array(12 + fingerprintBytes.byteLength + payloadBytes.byteLength);\n  buffer.set($claspBinaryMagic, 0);\n  $claspWriteUint32(buffer, 4, fingerprintBytes.byteLength);\n  $claspWriteUint32(buffer, 8, payloadBytes.byteLength);\n  buffer.set(fingerprintBytes, 12);\n  buffer.set(payloadBytes, 12 + fingerprintBytes.byteLength);\n  return buffer;\n}\n\nfunction $claspDecodeSchemaBinaryPayload(schemaContract, payload, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(payload, path);\n  if (bytes.byteLength < 12) {\n    throw new Error(`${path} expected a Clasp binary payload`);\n  }\n  for (let index = 0; index < $claspBinaryMagic.length; index += 1) {\n    if (bytes[index] !== $claspBinaryMagic[index]) {\n      throw new Error(`${path} expected a Clasp binary payload`);\n    }\n  }\n  const fingerprintLength = $claspReadUint32(bytes, 4, path);\n  const payloadLength = $claspReadUint32(bytes, 8, path);\n  const payloadOffset = 12 + fingerprintLength;\n  if (payloadOffset + payloadLength !== bytes.byteLength) {\n    throw new Error(`${path} has an invalid Clasp binary payload length`);\n  }\n  const fingerprint = $claspDecodeUtf8Text(bytes.slice(12, payloadOffset), `${path}.fingerprint`);\n  const expectedFingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  if (fingerprint !== expectedFingerprint) {\n    throw new Error(`${path} expected schema fingerprint ${expectedFingerprint} but received ${fingerprint}`);\n  }\n  const payloadText = $claspDecodeUtf8Text(bytes.slice(payloadOffset), `${path}.payload`);\n  return schemaContract.toHost(schemaContract.fromHost(JSON.parse(payloadText), path), path);\n}\n\nfunction $claspCreateSchemaBinaryCodec(schemaContract) {\n  return Object.freeze({\n    type: schemaContract.type ?? schemaContract.name ?? null,\n    schemaFingerprint: String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\"),\n    framing: \"length_prefixed\",\n    encode(value) { return $claspEncodeSchemaBinaryPayload(schemaContract, value); },\n    decode(bytes, path = \"value\") { return $claspDecodeSchemaBinaryPayload(schemaContract, bytes, path); },\n    frame(value) { return $claspFrameBinaryPayload(this.encode(value)); },\n    unframe(frame, path = \"value\") { return this.decode($claspUnframeBinaryPayload(frame, path), path); }\n  });\n}\n\nfunction $claspCreateSchemaStream(contract, initial = null) {\n  return Object.freeze({ kind: \"clasp-schema-stream\", schema: contract, initial });\n}\n\nfunction $claspPromptMessage(role, content) {\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([Object.freeze({\n      role: $claspExpectString(role, \"prompt.role\"),\n      content: $claspExpectString(content, \"prompt.content\")\n    })])\n  });\n}\n\nfunction $claspExpectPrompt(value, path = \"value\") {\n  const objectValue = $claspExpectObject(value, path);\n  if (objectValue.$kind !== \"prompt\") {\n    throw new Error(`${path} expected a Prompt value`);\n  }\n  const messages = $claspExpectArray(objectValue.messages, `${path}.messages`).map((message, index) => {\n    const messagePath = `${path}.messages[${index}]`;\n    const messageObject = $claspExpectObject(message, messagePath);\n    return Object.freeze({\n      role: $claspExpectString(messageObject.role, `${messagePath}.role`),\n      content: $claspExpectString(messageObject.content, `${messagePath}.content`)\n    });\n  });\n  return Object.freeze({ $kind: \"prompt\", messages: Object.freeze(messages) });\n}\n\nfunction systemPrompt(content) { return $claspPromptMessage(\"system\", content); }\nfunction assistantPrompt(content) { return $claspPromptMessage(\"assistant\", content); }\nfunction userPrompt(content) { return $claspPromptMessage(\"user\", content); }\n\nfunction appendPrompt(left, right) {\n  const leftPrompt = $claspExpectPrompt(left, \"left\");\n  const rightPrompt = $claspExpectPrompt(right, \"right\");\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([...leftPrompt.messages, ...rightPrompt.messages].map((message) => Object.freeze({ role: message.role, content: message.content })))\n  });\n}\n\nfunction promptText(value) {\n  const prompt = $claspExpectPrompt(value, \"value\");\n  return prompt.messages.map((message) => `${message.role}: ${message.content}`).join(\"\\n\\n\");\n}\n\nfunction $claspCreateTraceCollector(seed = []) {\n  const entries = Array.isArray(seed) ? seed.map((entry) => $claspSnapshotValue(entry)) : [];\n  return Object.freeze({\n    kind: \"clasp-trace-collector\",\n    version: 1,\n    record(entry) {\n      const snapshot = $claspSnapshotValue(entry);\n      entries.push(snapshot);\n      return snapshot;\n    },\n    entries() {\n      return $claspSnapshotValue(entries);\n    },\n    clear() {\n      entries.length = 0;\n      return this.entries();\n    }\n  });\n}\n\nfunction $claspMaybeRecordTrace(options = null, kind, name, action) {\n  const collector = options && typeof options === \"object\" && options.collector && typeof options.collector.record === \"function\"\n    ? options.collector\n    : null;\n  if (collector) {\n    collector.record(Object.freeze({ kind, name, action, status: \"ok\" }));\n  }\n}\nexport const defaultUser = { name: \"Ada\", active: true };\nexport function userName(user) { return (user).name; }\nexport const main = userName(defaultUser);\nconst __claspExports = Object.freeze({ defaultUser: defaultUser, userName: userName, main: main });\n\nconst $claspSchema_User = {\n  kind: \"clasp-schema-contract\",\n  version: 1,\n  name: \"User\",\n  schema: Object.freeze({ kind: \"record\", name: \"User\", fields: Object.freeze([\"name\", \"active\"]) }),\n  seed: null,\n  fromHost(value, path = \"value\") {\n    const objectValue = $claspExpectObject(value, path);\n    return Object.freeze({\n      name: $claspExpectString(objectValue.name, `${path}.name`),\n      active: $claspExpectBool(objectValue.active, `${path}.active`),\n    });\n  },\n  toHost(value, path = \"value\") { return this.fromHost(value, path); },\n  decodeJson(jsonText) { return this.fromHost(JSON.parse(jsonText), \"value\"); },\n  encodeJson(value) { return JSON.stringify(this.toHost(value, \"value\")); },\n  stream(initial = null) { return $claspCreateSchemaStream(this, initial); }\n};\n\nexport const __claspSchemas = Object.freeze({ User: $claspSchema_User });\nfor (const schemaContract of Object.values(__claspSchemas)) {\n  schemaContract.binary = $claspCreateSchemaBinaryCodec(schemaContract);\n  schemaContract.encodeBinary = function (value) { return this.binary.encode(value); };\n  schemaContract.decodeBinary = function (payload, path = \"value\") { return this.binary.decode(payload, path); };\n  schemaContract.encodeFramedBinary = function (value) { return this.binary.frame(value); };\n  schemaContract.decodeFramedBinary = function (frame, path = \"value\") { return this.binary.unframe(frame, path); };\n  Object.freeze(schemaContract);\n}\n\nexport const __claspTraceCollector = Object.freeze({\n  version: 1,\n  create(seed = []) { return $claspCreateTraceCollector(seed); }\n});\n\nexport const __claspModule = Object.freeze({\n  name: \"Records\",\n  version: 1\n});\n\nexport const __claspBindings = Object.freeze({\n  kind: \"clasp-generated-bindings\",\n  version: 1,\n  module: __claspModule,\n  schemas: __claspSchemas,\n  traces: __claspTraceCollector\n});\n","senaryParsedModuleName":"Decisions","senaryParsedTypeNames":["Decision"],"senaryParsedConstructorSummaries":["Keep Str Str","Drop"],"senaryChooseAnnotation":"Decision -> Str","senaryParsedDeclNames":["choose","main"],"senaryCheckedModule":"choose : Decision -> Str\nmain : Str","senaryLoweredModule":"function choose(decision) = match name:decision [Keep(left, right) -> name:left, Drop() -> literal:drop]\nconst main = call choose(ctor Keep(literal:alpha, literal:beta))","senaryEmittedModule":"// Generated by compiler-selfhost\nfunction $claspSnapshotValue(value) {\n  if (typeof globalThis.structuredClone === \"function\") {\n    return globalThis.structuredClone(value);\n  }\n  return value === undefined ? undefined : JSON.parse(JSON.stringify(value));\n}\n\nfunction $claspExpectObject(value, path = \"value\") {\n  if (typeof value !== \"object\" || value === null || Array.isArray(value)) {\n    throw new Error(`${path} expected an object`);\n  }\n  return value;\n}\n\nfunction $claspExpectArray(value, path = \"value\") {\n  if (!Array.isArray(value)) {\n    throw new Error(`${path} expected an array`);\n  }\n  return value;\n}\n\nfunction $claspExpectString(value, path = \"value\") {\n  if (typeof value !== \"string\") {\n    throw new Error(`${path} expected a string`);\n  }\n  return value;\n}\n\nfunction $claspExpectInt(value, path = \"value\") {\n  if (!Number.isInteger(value)) {\n    throw new Error(`${path} expected an integer`);\n  }\n  return value;\n}\n\nfunction $claspExpectBool(value, path = \"value\") {\n  if (typeof value !== \"boolean\") {\n    throw new Error(`${path} expected a boolean`);\n  }\n  return value;\n}\n\nconst $claspBinaryMagic = Object.freeze([67, 76, 66, 49]);\nconst $claspTextEncoder = typeof TextEncoder === \"undefined\" ? null : new TextEncoder();\nconst $claspTextDecoder = typeof TextDecoder === \"undefined\" ? null : new TextDecoder();\n\nfunction $claspEncodeUtf8Text(value) {\n  if ($claspTextEncoder === null) {\n    throw new Error(\"This runtime does not provide TextEncoder for binary Clasp codecs\");\n  }\n  return $claspTextEncoder.encode($claspExpectString(value, \"value\"));\n}\n\nfunction $claspDecodeUtf8Text(bytes, path = \"value\") {\n  if ($claspTextDecoder === null) {\n    throw new Error(\"This runtime does not provide TextDecoder for binary Clasp codecs\");\n  }\n  return $claspTextDecoder.decode($claspNormalizeBinaryBytes(bytes, path));\n}\n\nfunction $claspNormalizeBinaryBytes(value, path = \"value\") {\n  if (value instanceof Uint8Array) {\n    return value;\n  }\n  if (value instanceof ArrayBuffer) {\n    return new Uint8Array(value);\n  }\n  if (ArrayBuffer.isView(value)) {\n    return new Uint8Array(value.buffer, value.byteOffset, value.byteLength);\n  }\n  throw new Error(`${path} expected binary bytes`);\n}\n\nfunction $claspWriteUint32(target, offset, value) {\n  new DataView(target.buffer, target.byteOffset, target.byteLength).setUint32(offset, value, true);\n}\n\nfunction $claspReadUint32(source, offset, path = \"value\") {\n  if (offset + 4 > source.byteLength) {\n    throw new Error(`${path} truncated binary payload`);\n  }\n  return new DataView(source.buffer, source.byteOffset, source.byteLength).getUint32(offset, true);\n}\n\nfunction $claspFrameBinaryPayload(payload) {\n  const bytes = $claspNormalizeBinaryBytes(payload, \"payload\");\n  const frame = new Uint8Array(4 + bytes.byteLength);\n  $claspWriteUint32(frame, 0, bytes.byteLength);\n  frame.set(bytes, 4);\n  return frame;\n}\n\nfunction $claspUnframeBinaryPayload(frame, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(frame, path);\n  const expectedLength = $claspReadUint32(bytes, 0, path);\n  const payload = bytes.slice(4);\n  if (payload.byteLength !== expectedLength) {\n    throw new Error(`${path} expected ${expectedLength} framed bytes but received ${payload.byteLength}`);\n  }\n  return payload;\n}\n\nfunction $claspEncodeSchemaBinaryPayload(schemaContract, value) {\n  const normalizedValue = schemaContract.toHost(schemaContract.fromHost(value, \"value\"), \"value\");\n  const fingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  const fingerprintBytes = $claspEncodeUtf8Text(fingerprint);\n  const payloadBytes = $claspEncodeUtf8Text(JSON.stringify(normalizedValue));\n  const buffer = new Uint8Array(12 + fingerprintBytes.byteLength + payloadBytes.byteLength);\n  buffer.set($claspBinaryMagic, 0);\n  $claspWriteUint32(buffer, 4, fingerprintBytes.byteLength);\n  $claspWriteUint32(buffer, 8, payloadBytes.byteLength);\n  buffer.set(fingerprintBytes, 12);\n  buffer.set(payloadBytes, 12 + fingerprintBytes.byteLength);\n  return buffer;\n}\n\nfunction $claspDecodeSchemaBinaryPayload(schemaContract, payload, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(payload, path);\n  if (bytes.byteLength < 12) {\n    throw new Error(`${path} expected a Clasp binary payload`);\n  }\n  for (let index = 0; index < $claspBinaryMagic.length; index += 1) {\n    if (bytes[index] !== $claspBinaryMagic[index]) {\n      throw new Error(`${path} expected a Clasp binary payload`);\n    }\n  }\n  const fingerprintLength = $claspReadUint32(bytes, 4, path);\n  const payloadLength = $claspReadUint32(bytes, 8, path);\n  const payloadOffset = 12 + fingerprintLength;\n  if (payloadOffset + payloadLength !== bytes.byteLength) {\n    throw new Error(`${path} has an invalid Clasp binary payload length`);\n  }\n  const fingerprint = $claspDecodeUtf8Text(bytes.slice(12, payloadOffset), `${path}.fingerprint`);\n  const expectedFingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  if (fingerprint !== expectedFingerprint) {\n    throw new Error(`${path} expected schema fingerprint ${expectedFingerprint} but received ${fingerprint}`);\n  }\n  const payloadText = $claspDecodeUtf8Text(bytes.slice(payloadOffset), `${path}.payload`);\n  return schemaContract.toHost(schemaContract.fromHost(JSON.parse(payloadText), path), path);\n}\n\nfunction $claspCreateSchemaBinaryCodec(schemaContract) {\n  return Object.freeze({\n    type: schemaContract.type ?? schemaContract.name ?? null,\n    schemaFingerprint: String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\"),\n    framing: \"length_prefixed\",\n    encode(value) { return $claspEncodeSchemaBinaryPayload(schemaContract, value); },\n    decode(bytes, path = \"value\") { return $claspDecodeSchemaBinaryPayload(schemaContract, bytes, path); },\n    frame(value) { return $claspFrameBinaryPayload(this.encode(value)); },\n    unframe(frame, path = \"value\") { return this.decode($claspUnframeBinaryPayload(frame, path), path); }\n  });\n}\n\nfunction $claspCreateSchemaStream(contract, initial = null) {\n  return Object.freeze({ kind: \"clasp-schema-stream\", schema: contract, initial });\n}\n\nfunction $claspPromptMessage(role, content) {\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([Object.freeze({\n      role: $claspExpectString(role, \"prompt.role\"),\n      content: $claspExpectString(content, \"prompt.content\")\n    })])\n  });\n}\n\nfunction $claspExpectPrompt(value, path = \"value\") {\n  const objectValue = $claspExpectObject(value, path);\n  if (objectValue.$kind !== \"prompt\") {\n    throw new Error(`${path} expected a Prompt value`);\n  }\n  const messages = $claspExpectArray(objectValue.messages, `${path}.messages`).map((message, index) => {\n    const messagePath = `${path}.messages[${index}]`;\n    const messageObject = $claspExpectObject(message, messagePath);\n    return Object.freeze({\n      role: $claspExpectString(messageObject.role, `${messagePath}.role`),\n      content: $claspExpectString(messageObject.content, `${messagePath}.content`)\n    });\n  });\n  return Object.freeze({ $kind: \"prompt\", messages: Object.freeze(messages) });\n}\n\nfunction systemPrompt(content) { return $claspPromptMessage(\"system\", content); }\nfunction assistantPrompt(content) { return $claspPromptMessage(\"assistant\", content); }\nfunction userPrompt(content) { return $claspPromptMessage(\"user\", content); }\n\nfunction appendPrompt(left, right) {\n  const leftPrompt = $claspExpectPrompt(left, \"left\");\n  const rightPrompt = $claspExpectPrompt(right, \"right\");\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([...leftPrompt.messages, ...rightPrompt.messages].map((message) => Object.freeze({ role: message.role, content: message.content })))\n  });\n}\n\nfunction promptText(value) {\n  const prompt = $claspExpectPrompt(value, \"value\");\n  return prompt.messages.map((message) => `${message.role}: ${message.content}`).join(\"\\n\\n\");\n}\n\nfunction $claspCreateTraceCollector(seed = []) {\n  const entries = Array.isArray(seed) ? seed.map((entry) => $claspSnapshotValue(entry)) : [];\n  return Object.freeze({\n    kind: \"clasp-trace-collector\",\n    version: 1,\n    record(entry) {\n      const snapshot = $claspSnapshotValue(entry);\n      entries.push(snapshot);\n      return snapshot;\n    },\n    entries() {\n      return $claspSnapshotValue(entries);\n    },\n    clear() {\n      entries.length = 0;\n      return this.entries();\n    }\n  });\n}\n\nfunction $claspMaybeRecordTrace(options = null, kind, name, action) {\n  const collector = options && typeof options === \"object\" && options.collector && typeof options.collector.record === \"function\"\n    ? options.collector\n    : null;\n  if (collector) {\n    collector.record(Object.freeze({ kind, name, action, status: \"ok\" }));\n  }\n}\nexport function choose(decision) { return (() => {\n  const __match = decision;\n  if (__match[0] === \"Keep\") {\n    const [_, left, right] = __match;\n    return left;\n  }\n  if (__match[0] === \"Drop\") {\n    return \"drop\";\n  }\n  return undefined;\n})(); }\nexport const main = choose([\"Keep\", \"alpha\", \"beta\"]);\nconst __claspExports = Object.freeze({ choose: choose, main: main });\n\nexport const __claspSchemas = Object.freeze({  });\nfor (const schemaContract of Object.values(__claspSchemas)) {\n  schemaContract.binary = $claspCreateSchemaBinaryCodec(schemaContract);\n  schemaContract.encodeBinary = function (value) { return this.binary.encode(value); };\n  schemaContract.decodeBinary = function (payload, path = \"value\") { return this.binary.decode(payload, path); };\n  schemaContract.encodeFramedBinary = function (value) { return this.binary.frame(value); };\n  schemaContract.decodeFramedBinary = function (frame, path = \"value\") { return this.binary.unframe(frame, path); };\n  Object.freeze(schemaContract);\n}\n\nexport const __claspTraceCollector = Object.freeze({\n  version: 1,\n  create(seed = []) { return $claspCreateTraceCollector(seed); }\n});\n\nexport const __claspModule = Object.freeze({\n  name: \"Decisions\",\n  version: 1\n});\n\nexport const __claspBindings = Object.freeze({\n  kind: \"clasp-generated-bindings\",\n  version: 1,\n  module: __claspModule,\n  schemas: __claspSchemas,\n  traces: __claspTraceCollector\n});\n","septenaryParsedModuleName":"Lettings","septenaryParsedDeclNames":["describe","main"],"septenaryCheckedModule":"describe : Str -> Str\nmain : Str","septenaryLoweredModule":"function describe(name) = let alias = name:name in name:alias\nconst main = let current = call describe(literal:Ada) in name:current","septenaryEmittedModule":"// Generated by compiler-selfhost\nfunction $claspSnapshotValue(value) {\n  if (typeof globalThis.structuredClone === \"function\") {\n    return globalThis.structuredClone(value);\n  }\n  return value === undefined ? undefined : JSON.parse(JSON.stringify(value));\n}\n\nfunction $claspExpectObject(value, path = \"value\") {\n  if (typeof value !== \"object\" || value === null || Array.isArray(value)) {\n    throw new Error(`${path} expected an object`);\n  }\n  return value;\n}\n\nfunction $claspExpectArray(value, path = \"value\") {\n  if (!Array.isArray(value)) {\n    throw new Error(`${path} expected an array`);\n  }\n  return value;\n}\n\nfunction $claspExpectString(value, path = \"value\") {\n  if (typeof value !== \"string\") {\n    throw new Error(`${path} expected a string`);\n  }\n  return value;\n}\n\nfunction $claspExpectInt(value, path = \"value\") {\n  if (!Number.isInteger(value)) {\n    throw new Error(`${path} expected an integer`);\n  }\n  return value;\n}\n\nfunction $claspExpectBool(value, path = \"value\") {\n  if (typeof value !== \"boolean\") {\n    throw new Error(`${path} expected a boolean`);\n  }\n  return value;\n}\n\nconst $claspBinaryMagic = Object.freeze([67, 76, 66, 49]);\nconst $claspTextEncoder = typeof TextEncoder === \"undefined\" ? null : new TextEncoder();\nconst $claspTextDecoder = typeof TextDecoder === \"undefined\" ? null : new TextDecoder();\n\nfunction $claspEncodeUtf8Text(value) {\n  if ($claspTextEncoder === null) {\n    throw new Error(\"This runtime does not provide TextEncoder for binary Clasp codecs\");\n  }\n  return $claspTextEncoder.encode($claspExpectString(value, \"value\"));\n}\n\nfunction $claspDecodeUtf8Text(bytes, path = \"value\") {\n  if ($claspTextDecoder === null) {\n    throw new Error(\"This runtime does not provide TextDecoder for binary Clasp codecs\");\n  }\n  return $claspTextDecoder.decode($claspNormalizeBinaryBytes(bytes, path));\n}\n\nfunction $claspNormalizeBinaryBytes(value, path = \"value\") {\n  if (value instanceof Uint8Array) {\n    return value;\n  }\n  if (value instanceof ArrayBuffer) {\n    return new Uint8Array(value);\n  }\n  if (ArrayBuffer.isView(value)) {\n    return new Uint8Array(value.buffer, value.byteOffset, value.byteLength);\n  }\n  throw new Error(`${path} expected binary bytes`);\n}\n\nfunction $claspWriteUint32(target, offset, value) {\n  new DataView(target.buffer, target.byteOffset, target.byteLength).setUint32(offset, value, true);\n}\n\nfunction $claspReadUint32(source, offset, path = \"value\") {\n  if (offset + 4 > source.byteLength) {\n    throw new Error(`${path} truncated binary payload`);\n  }\n  return new DataView(source.buffer, source.byteOffset, source.byteLength).getUint32(offset, true);\n}\n\nfunction $claspFrameBinaryPayload(payload) {\n  const bytes = $claspNormalizeBinaryBytes(payload, \"payload\");\n  const frame = new Uint8Array(4 + bytes.byteLength);\n  $claspWriteUint32(frame, 0, bytes.byteLength);\n  frame.set(bytes, 4);\n  return frame;\n}\n\nfunction $claspUnframeBinaryPayload(frame, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(frame, path);\n  const expectedLength = $claspReadUint32(bytes, 0, path);\n  const payload = bytes.slice(4);\n  if (payload.byteLength !== expectedLength) {\n    throw new Error(`${path} expected ${expectedLength} framed bytes but received ${payload.byteLength}`);\n  }\n  return payload;\n}\n\nfunction $claspEncodeSchemaBinaryPayload(schemaContract, value) {\n  const normalizedValue = schemaContract.toHost(schemaContract.fromHost(value, \"value\"), \"value\");\n  const fingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  const fingerprintBytes = $claspEncodeUtf8Text(fingerprint);\n  const payloadBytes = $claspEncodeUtf8Text(JSON.stringify(normalizedValue));\n  const buffer = new Uint8Array(12 + fingerprintBytes.byteLength + payloadBytes.byteLength);\n  buffer.set($claspBinaryMagic, 0);\n  $claspWriteUint32(buffer, 4, fingerprintBytes.byteLength);\n  $claspWriteUint32(buffer, 8, payloadBytes.byteLength);\n  buffer.set(fingerprintBytes, 12);\n  buffer.set(payloadBytes, 12 + fingerprintBytes.byteLength);\n  return buffer;\n}\n\nfunction $claspDecodeSchemaBinaryPayload(schemaContract, payload, path = \"value\") {\n  const bytes = $claspNormalizeBinaryBytes(payload, path);\n  if (bytes.byteLength < 12) {\n    throw new Error(`${path} expected a Clasp binary payload`);\n  }\n  for (let index = 0; index < $claspBinaryMagic.length; index += 1) {\n    if (bytes[index] !== $claspBinaryMagic[index]) {\n      throw new Error(`${path} expected a Clasp binary payload`);\n    }\n  }\n  const fingerprintLength = $claspReadUint32(bytes, 4, path);\n  const payloadLength = $claspReadUint32(bytes, 8, path);\n  const payloadOffset = 12 + fingerprintLength;\n  if (payloadOffset + payloadLength !== bytes.byteLength) {\n    throw new Error(`${path} has an invalid Clasp binary payload length`);\n  }\n  const fingerprint = $claspDecodeUtf8Text(bytes.slice(12, payloadOffset), `${path}.fingerprint`);\n  const expectedFingerprint = String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\");\n  if (fingerprint !== expectedFingerprint) {\n    throw new Error(`${path} expected schema fingerprint ${expectedFingerprint} but received ${fingerprint}`);\n  }\n  const payloadText = $claspDecodeUtf8Text(bytes.slice(payloadOffset), `${path}.payload`);\n  return schemaContract.toHost(schemaContract.fromHost(JSON.parse(payloadText), path), path);\n}\n\nfunction $claspCreateSchemaBinaryCodec(schemaContract) {\n  return Object.freeze({\n    type: schemaContract.type ?? schemaContract.name ?? null,\n    schemaFingerprint: String(schemaContract.schema?.schemaFingerprint ?? schemaContract.type ?? schemaContract.name ?? \"value\"),\n    framing: \"length_prefixed\",\n    encode(value) { return $claspEncodeSchemaBinaryPayload(schemaContract, value); },\n    decode(bytes, path = \"value\") { return $claspDecodeSchemaBinaryPayload(schemaContract, bytes, path); },\n    frame(value) { return $claspFrameBinaryPayload(this.encode(value)); },\n    unframe(frame, path = \"value\") { return this.decode($claspUnframeBinaryPayload(frame, path), path); }\n  });\n}\n\nfunction $claspCreateSchemaStream(contract, initial = null) {\n  return Object.freeze({ kind: \"clasp-schema-stream\", schema: contract, initial });\n}\n\nfunction $claspPromptMessage(role, content) {\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([Object.freeze({\n      role: $claspExpectString(role, \"prompt.role\"),\n      content: $claspExpectString(content, \"prompt.content\")\n    })])\n  });\n}\n\nfunction $claspExpectPrompt(value, path = \"value\") {\n  const objectValue = $claspExpectObject(value, path);\n  if (objectValue.$kind !== \"prompt\") {\n    throw new Error(`${path} expected a Prompt value`);\n  }\n  const messages = $claspExpectArray(objectValue.messages, `${path}.messages`).map((message, index) => {\n    const messagePath = `${path}.messages[${index}]`;\n    const messageObject = $claspExpectObject(message, messagePath);\n    return Object.freeze({\n      role: $claspExpectString(messageObject.role, `${messagePath}.role`),\n      content: $claspExpectString(messageObject.content, `${messagePath}.content`)\n    });\n  });\n  return Object.freeze({ $kind: \"prompt\", messages: Object.freeze(messages) });\n}\n\nfunction systemPrompt(content) { return $claspPromptMessage(\"system\", content); }\nfunction assistantPrompt(content) { return $claspPromptMessage(\"assistant\", content); }\nfunction userPrompt(content) { return $claspPromptMessage(\"user\", content); }\n\nfunction appendPrompt(left, right) {\n  const leftPrompt = $claspExpectPrompt(left, \"left\");\n  const rightPrompt = $claspExpectPrompt(right, \"right\");\n  return Object.freeze({\n    $kind: \"prompt\",\n    messages: Object.freeze([...leftPrompt.messages, ...rightPrompt.messages].map((message) => Object.freeze({ role: message.role, content: message.content })))\n  });\n}\n\nfunction promptText(value) {\n  const prompt = $claspExpectPrompt(value, \"value\");\n  return prompt.messages.map((message) => `${message.role}: ${message.content}`).join(\"\\n\\n\");\n}\n\nfunction $claspCreateTraceCollector(seed = []) {\n  const entries = Array.isArray(seed) ? seed.map((entry) => $claspSnapshotValue(entry)) : [];\n  return Object.freeze({\n    kind: \"clasp-trace-collector\",\n    version: 1,\n    record(entry) {\n      const snapshot = $claspSnapshotValue(entry);\n      entries.push(snapshot);\n      return snapshot;\n    },\n    entries() {\n      return $claspSnapshotValue(entries);\n    },\n    clear() {\n      entries.length = 0;\n      return this.entries();\n    }\n  });\n}\n\nfunction $claspMaybeRecordTrace(options = null, kind, name, action) {\n  const collector = options && typeof options === \"object\" && options.collector && typeof options.collector.record === \"function\"\n    ? options.collector\n    : null;\n  if (collector) {\n    collector.record(Object.freeze({ kind, name, action, status: \"ok\" }));\n  }\n}\nexport function describe(name) { return (() => {\n  const alias = name;\n  return alias;\n})(); }\nexport const main = (() => {\n  const current = describe(\"Ada\");\n  return current;\n})();\nconst __claspExports = Object.freeze({ describe: describe, main: main });\n\nexport const __claspSchemas = Object.freeze({  });\nfor (const schemaContract of Object.values(__claspSchemas)) {\n  schemaContract.binary = $claspCreateSchemaBinaryCodec(schemaContract);\n  schemaContract.encodeBinary = function (value) { return this.binary.encode(value); };\n  schemaContract.decodeBinary = function (payload, path = \"value\") { return this.binary.decode(payload, path); };\n  schemaContract.encodeFramedBinary = function (value) { return this.binary.frame(value); };\n  schemaContract.decodeFramedBinary = function (frame, path = \"value\") { return this.binary.unframe(frame, path); };\n  Object.freeze(schemaContract);\n}\n\nexport const __claspTraceCollector = Object.freeze({\n  version: 1,\n  create(seed = []) { return $claspCreateTraceCollector(seed); }\n});\n\nexport const __claspModule = Object.freeze({\n  name: \"Lettings\",\n  version: 1\n});\n\nexport const __claspBindings = Object.freeze({\n  kind: \"clasp-generated-bindings\",\n  version: 1,\n  module: __claspModule,\n  schemas: __claspSchemas,\n  traces: __claspTraceCollector\n});\n"}