Benchmark harness instructions:
- Work only inside the current workspace.
- Do not inspect parent directories.
- Prefer the smallest local edit set that satisfies the task.
- Use the files in the workspace as the source of truth.
- Run `bash scripts/verify.sh` before finishing.
- Finish only after verification passes.

# Task: Upgrade the lead summary flow to a LeadProfile contract

The lead follow-up slice needs a profile-oriented contract upgrade that should propagate through the API, workflow state, and downstream lookup request.

## Requirements
1. Introduce a new record type named `LeadProfile` with this exact schema: `company : Str`, `segment : Str`, `plan : Str`, `ownerTeam : Str`.
2. Update the downstream playbook lookup request so it carries `segment : Str` and `ownerTeam : Str` instead of a single status string.
3. Update the workflow state so it stores `profile : LeadProfile`.
4. Update the lead summary API route so it returns `LeadProfile`.
5. Keep the program compiling.
6. Keep `main` working. After the change, `main` should evaluate to `"enterprise"`.

## Acceptance
The task is complete when `bash scripts/verify.sh` passes.

## Working Guidance
- Discover the relevant files from the workspace.
- Do not assume the exact edit surface before reading the local files.

## Compiler Guidance
- The semantic brief below is compiler-derived from the current workspace.
- Treat it as authoritative for scoping and type relationships unless verification proves otherwise.
- Prefer editing the typed dependency chain it names before doing broader repo discovery.
- Use existing source syntax in the workspace as the syntax reference.

# Clasp Semantic Brief
Use this as the current typed dependency graph. Prefer it over rediscovering the same relationships from scratch.

## Compiler-Known Current Surfaces
Schemas:
- LeadIntake @ Shared/Lead.clasp:7 => { company:Str, priority:LeadPriority }
- LeadSummary @ Shared/Lead.clasp:12 => { company:Str, priorityLabel:Str, status:Str }
- LeadPlaybookLookup @ Shared/Lead.clasp:18 => { status:Str }
- LeadPlaybook @ Shared/Lead.clasp:22 => { guidance:Str }
- LeadFollowUpState @ Shared/Lead.clasp:26 => { summary:LeadSummary }
Routes:
- summarizeLeadApi @ Main.clasp:37 => POST /api/lead/summary LeadIntake -> LeadSummary
Tools:
- lookupLeadPlaybook @ Main.clasp:11 => LeadPlaybookLookup -> LeadPlaybook
Workflows:
- LeadFollowUpFlow => state LeadFollowUpState
Decls:
- summarizeLead : LeadIntake -> LeadSummary
- playbookRequest : LeadSummary -> LeadPlaybookLookup
- main : Str

## Verifier-Critical Targets
- Required schema after edits: LeadProfile => { company:Str, ownerTeam:Str, plan:Str, segment:Str }
- Required schema after edits: LeadFollowUpState => { profile:LeadProfile }
- Remove old schema name from emitted artifacts: LeadSummary
- Tool lookupLeadPlaybook must use LeadPlaybookLookup fields { ownerTeam:Str, segment:Str } and return LeadPlaybook
- Route summarizeLeadApi must return LeadProfile
- Workflow LeadFollowUpFlow must keep state LeadFollowUpState.profile:LeadProfile