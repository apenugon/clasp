{"summary":"Added native CLI scenario coverage for the empty-objective `swarm manager next` flow so the new `empty` / `plan-tasks` behavior is exercised end to end in both JSON and text modes.","files_touched":["scripts/test-native-claspc.sh"],"tests_run":["nix develop -c cargo test --manifest-path runtime/Cargo.toml swarm_manager_next_keeps_empty_objectives_actionable -- --nocapture","Manual native CLI smoke scenario via `claspc swarm objective create` + `claspc swarm manager next` asserting empty-objective JSON and text output"],"residual_risks":["`runtime/swarm.rs` was already modified in the workspace before this change; I did not alter it in this pass.","I did not wait for a full `nix develop -c bash scripts/test-native-claspc.sh` run to finish after the targeted fix, so only the narrow scenario-level coverage was confirmed."],"feedback":{"summary":"Verifier feedback addressed by adding scenario-level native CLI coverage for the empty-objective manager path.","ergonomics":["Kept the change local to the existing swarm SQLite scenario block in `scripts/test-native-claspc.sh`.","Covered both machine-facing JSON fields and the operator-facing suggested command text to match the control-plane surface change."],"follow_ups":["If the verifier wants broader confidence, rerun the full native CLI script after this targeted pass.","Once the verifier is satisfied, include this path in the repo-wide verifier-owned gate."],"warnings":["The workspace remains dirty outside this patch; unrelated changes were left intact.","No repo-wide verification was run, per task instructions."]}}