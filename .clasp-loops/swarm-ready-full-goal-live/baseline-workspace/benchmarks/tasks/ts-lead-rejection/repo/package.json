{
  "name": "ts-lead-priority-benchmark",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "npm run clean && npm run build && bun test/rejection.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
