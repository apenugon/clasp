Benchmark harness instructions:
- Work only inside the current workspace.
- Do not inspect parent directories.
- Prefer the smallest local edit set that satisfies the task.
- Use the files in the workspace as the source of truth.
- Run `bash scripts/verify.sh` before finishing.
- Finish only after verification passes.

# Task

The lead follow-up slice needs a contract upgrade.

Make the following product change:

1. Rename `LeadSummary` to `LeadDigest`.
2. Replace the old digest fields with this exact schema:
   - `company : Str`
   - `priorityLabel : Str`
   - `owner : Str`
   - `needsFollowUp : Bool`
3. Update `LeadPlaybookLookup` so it no longer uses `status`.
   It should now use:
   - `owner : Str`
   - `needsFollowUp : Bool`
4. Update the workflow state so it stores a `digest : LeadDigest`.
5. Update the route so `summarizeLeadApi` returns `LeadDigest`.
6. Keep the program compiling.
7. Keep `main` working. After the change, `main` should evaluate to `"senior-ae"`.

The validator will check that the upgrade propagated through:

- shared schemas
- route metadata
- tool metadata
- workflow metadata
- context graph output
- the compiled module's `main`

Compiler-derived semantic brief:
# Task
# Task

The lead follow-up slice needs a contract upgrade.

Make the following product change:

1. Rename `LeadSummary` to `LeadDigest`.
2. Replace the old digest fields with this exact schema:
   - `company : Str`
   - `priorityLabel : Str`
   - `owner : Str`
   - `needsFollowUp : Bool`
3. Update `LeadPlaybookLookup` so it no longer uses `status`.
   It should now use:
   - `owner : Str`
   - `needsFollowUp : Bool`
4. Update the workflow state so it stores a `digest : LeadDigest`.
5. Update the route so `summarizeLeadApi` returns `LeadDigest`.
6. Keep the program compiling.
7. Keep `main` working. After the change, `main` should evaluate to `"senior-ae"`.

The validator will check that the upgrade propagated through:

- shared schemas
- route metadata
- tool metadata
- workflow metadata
- context graph output
- the compiled module's `main`

# Clasp Semantic Brief
Schemas:
- LeadIntake @ Shared/Lead.clasp:7 => { company:Str, priority:LeadPriority }
- LeadSummary @ Shared/Lead.clasp:12 => { company:Str, priorityLabel:Str, status:Str }
- LeadPlaybookLookup @ Shared/Lead.clasp:18 => { status:Str }
- LeadPlaybook @ Shared/Lead.clasp:22 => { guidance:Str }
- LeadFollowUpState @ Shared/Lead.clasp:26 => { summary:LeadSummary }
Routes:
- summarizeLeadApi @ Main.clasp:37 => POST /api/lead/summary LeadIntake -> LeadSummary
Tools:
- lookupLeadPlaybook @ Main.clasp:11 => LeadPlaybookLookup -> LeadPlaybook
Workflows:
- LeadFollowUpFlow => state LeadFollowUpState
Decls:
- summarizeLead : LeadIntake -> LeadSummary
- playbookRequest : LeadSummary -> LeadPlaybookLookup
- main : Str
Verification artifacts:
- context nodes=52 edges=47
- air roots=61 nodes=104