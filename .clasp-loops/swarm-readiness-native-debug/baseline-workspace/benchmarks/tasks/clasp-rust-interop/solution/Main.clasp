module Main

record LeadRequest = {
  company : Str,
  budget : Int
}

record LeadSummary = {
  summary : Str,
  accepted : Bool
}

foreign mockLeadSummaryModel : LeadRequest -> Str = "mockLeadSummaryModel"

main : Str
main = "ok"
